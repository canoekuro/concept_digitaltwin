- そもそも生成AIのため回答に再現性が無いので、promptのバージョン管理をする必要が薄い。かつ、ユーザー側で色々とプロンプトをいじることが想定されるので、template_versionでの管理は廃止する。
- アプリ構成について、UI側で調査定義→ジョブ側で実行→UI側で確認という形になっているが、ジョブ側の実行に関してはnotebook化して、定義がvolumeに格納されたらそれをキック＋入力として稼働するようにする。
