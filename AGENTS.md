# Project Guidelines (SSoT)

このファイルはプロジェクト固有のガイドラインを記述する場所です。
技術的な決定事項、アーキテクチャ、コマンドなどをここに追記してください。

## 参照先
- **行動規範:** `.agents/rules/` を参照してください。
- **ワークフロー:** `.agents/workflows/` を参照してください。
- **スキル:** `.agents/skills/` を参照してください（テスト実行用スキル `.agents/skills/run-tests/SKILL.md` を含む）。
- **プロジェクト概要（親仕様）:** `SPEC.md` を参照してください。
- **フェーズ1 構築仕様:** `SPEC_PHASE1.md` を参照してください（実装時はこちらが優先）。
- **調査定義の書き方:** `docs/GUIDE_SURVEY_DEFINITION.md`（利用者向けの手引き）。

## 開発コマンド
- **テスト実行**: `./scripts/run-tests.sh` （環境依存を吸収したテスト実行スクリプト）

## このプロジェクトについて

AIペルソナによる**コンセプト調査**シミュレータ。LLM で生活者ペルソナ群を仮想再現し、
コンセプト受容性の定量アンケートをシミュレーションする社内システム。

`research_system` のコンセプト調査部分（`persona_sim` ＋ Web UI）を切り出したもの。
JTBD 仮説探索（`persona_jtbd`）は移していないので、このリポジトリには入れないこと。

現在地は **フェーズ1（パイプライン構築）**。`SPEC_PHASE1.md` §12 の実装順序
（M1 `personas_base` → M2 調査定義パーサ → M3 実行エンジン → M4 スクリーニング →
M5 集計・出力 → M6 冪等性・メタデータ → M7 並列化）に沿って進める。

パイプラインの上に **コンセプト調査 Web UI**（Streamlit / Databricks Apps）を載せる。
仕様は `docs/SPEC_UI.md`。UI は調査定義を組み立てて既存の実行経路に渡すだけで、
パネル構築・回答生成・集計のロジックを持たない。

## 仕様の優先順位（重要）

仕様書はスコープが異なる。**矛盾したらフェーズ1仕様が優先**する。

| 文書 | 位置づけ |
|---|---|
| `SPEC.md` | 親仕様。全体構想・フェーズ2以降・ガバナンス・非機能要件の SSoT |
| `SPEC_PHASE1.md` | フェーズ1の構築仕様。`SPEC.md` §16 を差し替える。**実装の直接の根拠** |
| `docs/SPEC_UI.md` | コンセプト調査 Web UI の仕様。`SPEC_PHASE1.md` §10.3 を展開したもの |

- 実行環境はフェーズ1では **Azure AI Foundry / Databricks**（`SPEC_PHASE1.md`）。
  `SPEC.md` §5・§13 のローカル vLLM 構成はフェーズ2以降の想定として扱う。
- フェーズ1の責務境界は「指定条件どおりにペルソナが回答し、集計表を出す」まで。
  精度検証・補正・示唆出しは利用側の作業（`SPEC_PHASE1.md` §1.2）。

## 実装上の不変条件（違反したら結果の意味が変わる）

`SPEC_PHASE1.md` §13 より。レビュー時は必ずここを確認する。

- **思考モード（thinking）は常に OFF。** 回答が収束しペルソナ間のばらつきが失われる。
  理由を書かせたい場合は `questions[].reasoning`（`SPEC_PHASE1.md` §6.3）を使う。
  思考の内容が `responses.answer_reasoning` に残り、予算と記録で抑えられる点が違う。
  **ばらつきが縮む懸念は同じ**なので、使ったら分布の形とセグメント間の差を必ず確かめる。
- **`temperature` などのサンプリングパラメータは指定しない**（`SPEC_PHASE1.md` §6.3）。
  最新モデルでは無視・拒否されうるため、指定すると「設定したつもり」の記録だけが残り
  実挙動と食い違う。エンドポイント既定に任せ、調査定義に書かれていたら理由つきで停止する。
- **順序尺度の選択肢はシャッフルしない。** `randomize_options: true` は非順序の単一回答のみ。
  シャッフルした場合は実際の提示順を `options_order` に必ず記録する。
- **同一ペルソナは同一コンセプトに1回しか回答しない。** パネル内で `persona_uuid` は一意、
  `assigned_stimuli` に重複を含めない（`SPEC_PHASE1.md` §5.0）。
- **提示設計は反実仮想モナディック固定。設定で変えられるようにしない。** 全ペルソナが
  全コンセプトを定義順に1件ずつ評価する。「一部の人にだけ見せる」「全案を同時に見せる」
  「提示順を入れ替える」を設定で選べるようにすると、コンセプト間でセグメント構成が
  ずれる経路と、順序効果が入る経路が同時に戻る。
- **`screened_out` レコードを削除しない。** インシデンス検証に使う。
- **スクリーニング確定後に `cell_rank` を振り直す**（`SPEC_PHASE1.md` §4.2）。
  オーバーサンプル時の順位のまま残すと、非通過で空いた穴がそのまま順位の飛びになり、
  「セル内の何番目に選ばれた人か」がパネルの記録から読めなくなる。
- **スクリーナーの通過率を実測値として扱わない。** 本人には聞いておらず、別の判定で
  条件合致の蓋然性が高いとされた割合にすぎない。`screener_incidence_estimated` に記録し、
  実測値の欄を作らない。判定結果には `inferred` フラグを必ず立てて実回答と区別する
  （`SPEC_PHASE1.md` §4.2）。スクリーニングを行っていない調査の通過率は 1.0 ではなく
  未測定（`null`）。
- **`seed` と各バージョンが揃えば、モデルへの入力が完全に再現できること。** 満たせない実装は不可。
  誰に・どのコンセプトを・何番目に・どの選択肢順で・どのプロンプトで聞いたかまでが対象
  （`SPEC_PHASE1.md` §9.1）。**生成結果そのものの一致は保証しない**（エンドポイント側の
  非決定性は除去できない）。だから `answer_raw` / `attempt` / `flags` を必ず残す。
- 品質フラグは立てるだけで **除外はしない**。集計側で選べるようにする。
- パース失敗・拒否が閾値を超えたら、止めずに **必ず表面化させる**（`SPEC_PHASE1.md` §11 E3/E4）。

## 禁止事項（Out of Scope / `SPEC.md` §2.2）

要求されても実装しない。必要なら仕様側の変更提案から始める。

- 実査の完全代替（統計的検定・市場規模の絶対値推計）
- 実在する特定個人の再現・意思決定モデル化
- 広告効果の絶対値予測（相対比較のみ）
- 出力に統計的推測の語彙（p値・有意差・信頼区間）を使うこと

## データ・法務の順守事項

- Nemotron-Personas-Japan は **CC BY 4.0**。帰属表示が義務（`SPEC.md` §15.3）。
  レポート出力・About 表示に帰属文を必ず含める。
- 社内データは **セグメント統計に集約してから** 使う。個票を直接ペルソナに紐づけない。
- 秘密情報（APIキー・トークン・顧客名・内部ホスト名）をコード・コミット・
  ログ・仕様書に残さない。資格情報は環境変数／シークレット管理経由で読む。
- **サービングエンドポイント名は設定値として書いてよい。** 秘密ではなく、実際に何で回したかが
  読み取れないと再現できないため（`SPEC_PHASE1.md` §9.1）。調査定義・`config/ui_config.yaml`・
  仕様書に実名を書く。
- **カタログ名・スキーマ名はコードに書かない。** テーブルの置き場所は環境変数
  （`PERSONA_SIM_CATALOG` / `PERSONA_SIM_SCHEMA` / `PERSONA_SIM_WAREHOUSE`）からのみ解決する
  （`persona_sim/config.py`）。
- **デプロイ設定（`app.yaml` / Bundle）に配置先を書くのは可。** 秘密ではなく、
  どのカタログに書いたのかが読み取れないと結果を辿れないため（エンドポイント名と同じ理由）。
  環境変数の値を**入れる**側であって、コードから読む側ではないので上の規則と両立する。

## Claude Code ネイティブ機能（PDCA + サブエージェント委譲）

- **PDCA ワークフロー:** `/pdca`（`.claude/commands/pdca.md`）。レビュー→計画→改修→コミットの
  改善サイクルを、メインセッションがオーケストレーションしながら回す。
- **全体レビュー:** `/codebase-review`（`.claude/commands/codebase-review.md`）。読み取り専用で
  所見ドキュメントを `docs/issues/` に生成し、修正は `/pdca` に引き継ぐ。
- **実装サブエージェント:** `.claude/agents/` 配下。トークン節約のため実装を委譲する。
  - `implementer-sonnet`（Sonnet）: ルーチン・標準実装。
  - `implementer-opus`（Opus）: 難易度の高い実装。
  - 最難所はメインセッションが直接実装する。
- **役割分担の規範:** `.agents/rules/agent-orchestration.md`（常時ON）。メインは設計・監査・
  レビューに専念し、実装は上記サブエージェントへ委譲、成果は監査後にコミットする。

## 記録

- 実装・仕様・設定を変更したら `docs/history/YYYYMMDD-HHMMSS-theme-{plan,result}.md` を
  ペアで保存し、`CHANGELOG.md` の履歴一覧に1エントリ追記する
  （`.agents/rules/plan-before-modify.md`）。
