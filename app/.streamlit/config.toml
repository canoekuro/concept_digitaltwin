# ライトモードの見た目調整（docs/issues/UIの修正.md）。
# 色だけを変える Streamlit 標準機能。複雑なCSSは使わない。
[theme]
base = "light"
primaryColor = "#0071e3"
backgroundColor = "#f5f5f7"
secondaryBackgroundColor = "#ffffff"
textColor = "#1d1d1f"
font = "sans serif"
