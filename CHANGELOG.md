# CHANGELOG (変更履歴)

このリポジトリにおける主要な開発マイルストーンの計画および実施結果の目次です。詳細は `docs/history/` に保存されている各マークダウンファイルを参照してください。

---

## 履歴一覧 (昇順)

### 1. リポジトリ基盤とエージェント実行環境の立ち上げ
- **日付**: 2026/07/27 00:15
- **計画**: [docs/history/20260727-001500-repo-bootstrap-plan.md](docs/history/20260727-001500-repo-bootstrap-plan.md)
- **結果**: [docs/history/20260727-001500-repo-bootstrap-result.md](docs/history/20260727-001500-repo-bootstrap-result.md)
- **概要**: 仕様書2点（`SPEC.md` / `SPEC.md`）の配置と、`retrospective` / `shelf_gaze_ml` に準拠した Claude Code 実行環境（`.agents/{rules,workflows,skills}`、`.claude/{agents,commands,hooks,settings.json}`、`AGENTS.md`）、および CI・記録規約の整備。実装（M1）は未着手。

### 2. M1 `personas_base` / M2 ジョブ定義・validate・panel の実装
- **日付**: 2026/07/27 01:30
- **計画**: [docs/history/20260727-013000-m1-m2-personas-panel-plan.md](docs/history/20260727-013000-m1-m2-personas-panel-plan.md)
- **結果**: [docs/history/20260727-013000-m1-m2-personas-panel-result.md](docs/history/20260727-013000-m1-m2-personas-panel-result.md)
- **概要**: `persona_sim` パッケージを新設し、`personas_base` の取り込み・正規化（M1）と、ジョブ定義パーサ・`validate`・`panel`（M2）を実装。あわせて `SPEC.md` を 0.7 に改訂し、提示設計を `design.type` から**3軸**（`sample_overlap` / `presentation` / `memory`）へ置き換えた。既定は反実仮想モナディック。抽出は乱数を使わず決定論的ハッシュ順とし、seed 再現性と並列度非依存をテストで担保。実データ1シャード（125,000 行）で端から端まで検証済み。

### 3. M3 実行エンジン / M6 冪等性・品質フラグ・実行メタデータの実装
- **日付**: 2026/07/27 02:30
- **計画**: [docs/history/20260727-023000-m3-m6-run-engine-plan.md](docs/history/20260727-023000-m3-m6-run-engine-plan.md)
- **結果**: [docs/history/20260727-023000-m3-m6-run-engine-result.md](docs/history/20260727-023000-m3-m6-run-engine-result.md)
- **概要（M3/M6）**: `persona-sim run` を実装。推論基盤は **Databricks Model Serving**（Databricks Apps での提供を見据えた選定）で、決定論的な Fake クライアントにより実エンドポイント無しでもパイプラインを通せる。実行単位を「セッション」とし `design.memory` がその粒度を決める。再開もセッション粒度で行い、途中まで進んだセッションは丸ごとやり直す（履歴の欠けた回答を残さないため）。品質フラグ5種・E3/E4 警告・E5 中断・`run_metadata.json` を追加。`SPEC.md` を 0.8 に改訂し、§9.1 で**再現性の範囲**（入力側は保証、生成結果の一致は保証しない）を明記した。

### 4. クイックスタート notebook の追加
- **日付**: 2026/07/27 03:00
- **計画**: [docs/history/20260727-030000-quickstart-notebook-plan.md](docs/history/20260727-030000-quickstart-notebook-plan.md)
- **結果**: [docs/history/20260727-030000-quickstart-notebook-result.md](docs/history/20260727-030000-quickstart-notebook-result.md)
- **概要**: 環境セットアップから結果までを1本で通す `notebooks/quickstart.ipynb` を追加。先頭で Databricks かローカルかを判定し、依存の入れ方とテーブル配置を分岐する（`SPEC.md` §10.2 のノートブック運用に対応）。既定は `model.endpoint: fake` なので推論エンドポイント無しでも完走する。簡易集計は `options_order` で定義順に戻してから行い、M5 の正式なクロス集計表とは別物であることを明記した。nbclient で全35セルを実際に実行し、エラー0件で完走することを確認済み。

### 5. M4 スクリーニング機構の実装
- **日付**: 2026/07/27 06:00
- **計画**: [docs/history/20260727-060000-m4-screening-plan.md](docs/history/20260727-060000-m4-screening-plan.md)
- **結果**: [docs/history/20260727-060000-m4-screening-result.md](docs/history/20260727-060000-m4-screening-result.md)
- **概要（M4）**: 対象者条件を **2方式（`ask` / `assume`）の選択式**で実装。`ask` は実際に聞いて通過者を残しインシデンスを実測、`assume` は聞かずに条件を前提として与え費用0（インシデンスは測れないので `null` を記録し、1.0 とは書かない）。どちらも確定したペルソナのカード末尾に前提ブロックを付ける（`memory: none` では会話履歴で引き継げないため）。`persona-sim screen` を新設し `panel` は LLM を呼ばないまま保った。確定後に `cell_rank` を振り直してから割り当てる不変条件を追加（振り直さないと `disjoint` の均等割り当てが崩れる）。`SPEC.md` を 0.9 に改訂し、§2.6 `screener_responses` を新設。

### 6. M5 集計・出力（クロス集計表・コンセプト比較表・xlsx）
- **日付**: 2026/07/27 23:45
- **計画**: [docs/history/20260727-234500-m5-aggregate-output-plan.md](docs/history/20260727-234500-m5-aggregate-output-plan.md)
- **結果**: [docs/history/20260727-234500-m5-aggregate-output-result.md](docs/history/20260727-234500-m5-aggregate-output-result.md)
- **概要**: `persona_sim/aggregate` を新設し、`persona-sim aggregate` / `export` を実装。`SPEC.md` §7.4 の出力一式（クロス集計表・コンセプト比較表・自由回答・パネル構成・生データ・`report.xlsx`）と `aggregates` テーブル（§2.5）を出す。集計の算術は pyspark に依存させず、Spark 抜きで手計算と突き合わせられるようにした。人が読む整形値（CSV / xlsx）と機械可読な生の値（`aggregates`）を分け、`export` は後者から表を組み直す。あわせて `multi` 設問の記録の欠落（`answer_codes` が保存されず、必ず `parse_error` が立って再開のたびにやり直していた）を修正し、`SPEC.md` を 1.0 に改訂した。これでフェーズ1は M7 を除いて実装済み。

### 7. Databricks の content ブロック対応と quickstart の並べ替え
- **日付**: 2026/07/28 11:00
- **計画**: [docs/history/20260728-110000-issue-202607281042-plan.md](docs/history/20260728-110000-issue-202607281042-plan.md)
- **結果**: [docs/history/20260728-110000-issue-202607281042-result.md](docs/history/20260728-110000-issue-202607281042-result.md)
- **概要**: `docs/issues/202607281042.md` の2件に対応。(1) Databricks の `message.content` が文字列とは限らず、Gemini 系は content part の配列を返すため、`str(content)` によって Python のリスト repr が `answer_raw` / `answer_text` に流れていた。推論クライアントの境界で本文だけを取り出すようにし、取り出せないときは repr にフォールバックせず `LLMError` にした（自由回答が読めなくなるだけでなく、正規表現フォールバックに落ちた選択式で `thoughtSignature` の base64 から数字を拾う危険があった）。LLM クライアントのテストを新設。(2) `notebooks/quickstart.ipynb` のスクリーニングが末尾に別立てされていたのを本流に組み込み、定義は手順6のジョブ定義に、実行は手順8（panel）と手順10（run）のあいだの手順9に移して `validate → panel → screen → run` を1本の流れにした。並べ替えて表に出た「候補段階では `assigned_stimuli` が空」という説明の食い違いもあわせて直した。実データ125,000行・全42セルを実際に通して確認済み。あわせて `model.thinking` がエンドポイントに送られていない件を `docs/issues/202607281100.md` に切り出した（実装は見送り）。

### 8. 用語の survey 統一・プロンプトの config 化と可視化・infer スクリーニング方式
- **日付**: 2026/07/28 14:30
- **計画**: [docs/history/20260728-143000-issue-202607281420-plan.md](docs/history/20260728-143000-issue-202607281420-plan.md)
- **結果**: [docs/history/20260728-143000-issue-202607281420-result.md](docs/history/20260728-143000-issue-202607281420-result.md)
- **概要**: `docs/issues/202607281420.md` の7件に対応。**破壊的変更を2件含み、既存の Delta テーブルはすべて作り直しが必要**。(1) 「ジョブ」は社内で別の意味を持つ語なので、中心概念を `survey`／調査に統一した。永続列 `job_id` → `survey_id`、トップレベルキー `job:` → `survey:` まで及び、旧キーは書き換え方を添えて停止する。(2) `answer_code` は `answer_codes` の射影で `multi` では常に null だったため廃止し、集計経路が割れないよう一本化した。(3) `[system]`・見出し・設問タイプ別の指示行を `prompt` 配下から差し替え可能にし、上書きがあるのに `template_version` が既定のままなら警告する。解決できない差し込みは読み込み時に停止する。(4) 設定値だけでは「結局どう聞いたのか」が読めないため、実行後に1ペルソナ分の実プロンプトを `prompt_sample.md` と実行メタデータに残す（純関数なので LLM を呼ばず組み直せる。実際に送った内容と一致することをテストで確認）。(5)(6) quickstart に `prompt` と `prompt_sample.md` 表示セルを追加し、`image_uri` を Unity Catalog Volumes 前提に改めて形式を検査する。(7) `ask` の費用と `assume` の矛盾の中間として **`screener.mode: infer`** を追加。候補を `batch_size` 人ずつまとめて別の LLM に渡し、条件合致の蓋然性が高い者にだけ属性を付与する。判定モデルと判定に見せるペルソナ情報を制御でき、パネル確定以降は `ask` と同じ経路を通す。通過率は**推定値**として `screener_incidence_estimated` に分けて記録し、判定結果には `inferred` フラグを立てて実回答と区別する。`endpoint: fake` で3方式すべてを端から端まで通して確認済み。

### 9. 仕様書とコードの整合化および image_mode: native の実装
- **日付**: 2026/07/28 20:34
- **計画**: [docs/history/20260728-202800-spec-code-alignment-plan.md](docs/history/20260728-202800-spec-code-alignment-plan.md)
- **結果**: [docs/history/20260728-202800-spec-code-alignment-result.md](docs/history/20260728-202800-spec-code-alignment-result.md)
- **概要**: `SPEC.md`、`SPEC.md`、および `persona_sim` コードベースの全領域における乖離を解消。(1) `SPEC.md` を更新し、`azure_ai_foundry` がフェーズ1対象外であることを明記し、`estimated_cost` 仕様（`null`固定＋トークン数記録）および `screener.infer` 構造、`run_metadata.json` 拡張項目を反映。(2) `SPEC.md` を更新し、`SPEC.md` が優先である旨を明記した上で、`job_id` → `survey_id` 統合および 3軸提示設計への移行注記を追加。(3) `persona_sim` コード側で `image_mode: native`（マルチモーダル提示）の動作をサポート。`ChatMessage` で `list[dict[str, Any]]` content parts を保持可能にし、プロンプト組み立て・`DatabricksServingClient`・`FakeLLMClient` でマルチモーダルメッセージを正しく展開・応答できるよう改修。`validate.py` の未実装エラーを解除し、全 305 件のテストがグリーンで通過することを確認。

### 10. temperature パラメータの廃止
- **日付**: 2026/07/28 21:00
- **計画**: [docs/history/20260728-210000-remove-temperature-plan.md](docs/history/20260728-210000-remove-temperature-plan.md)
- **結果**: [docs/history/20260728-210000-remove-temperature-result.md](docs/history/20260728-210000-remove-temperature-result.md)
- **概要**: 最新の推論モデルでは `temperature` の指定が非推奨・無効化される傾向にあり、指定しても効かないまま「設定したつもり」の値だけが調査定義と `run_metadata.json` に残って実挙動と食い違う。そこで指定を全面的にやめ、サンプリングはエンドポイント既定に委ねる方針に変更した。**調査定義の破壊的変更を含む**（既存 Delta テーブルの作り直しは不要）。(1) `LLMClient.complete()` の契約から `temperature` を外し、Databricks サービングエンドポイントへ送る payload にも載せない。(2) `ModelConfig` / `InferModelOverrides` から削除し、`model.temperature` / `infer.model.temperature` が書かれていたら**廃止理由を添えて停止**する（既存の `REMOVED_DESIGN_FIELDS` と同じ機構。黙って無視すると「指定したつもり」が `survey_definition` に記録として残るため）。(3) §6.3 のパース失敗リトライから **temperature 昇温を廃止**し、同じ入力の再送に変更した（仕様は 0.0 → 0.3 と書きつつ実装は 1.0 だった乖離も、この削除で解消）。(4) `run_metadata.json` の `model` ブロックと判定モデルブロックから、送っていないパラメータとしてキーごと削除。(5) `SPEC.md` を 1.1 に改訂し、§6.3 に方針、§9.1 の再現性の記述、§13 の実装上の注意（および `AGENTS.md` の不変条件）を更新。`SPEC.md` §8.2・§17 R4 も追随させた。

### 11. コンセプト調査 Web UI のルール改定と screener スキーマ再設計
- **日付**: 2026/07/28 16:00
- **計画**: [docs/history/20260728-160000-concept-survey-ui-schema-plan.md](docs/history/20260728-160000-concept-survey-ui-schema-plan.md)
- **結果**: [docs/history/20260728-160000-concept-survey-ui-schema-result.md](docs/history/20260728-160000-concept-survey-ui-schema-result.md)
- **概要**: 追加された Web UI 仕様書をレビューし、食い違いのうち**ルール・スキーマの方を直すべきもの**を改定した。**調査定義の破壊的変更を含む**（既存 Delta テーブルの作り直しは不要）。(1) `AGENTS.md` の秘密情報の定義から**サービングエンドポイント名を外した**。秘密ではないうえ、何で回したかが読み取れないと再現できない（§9.1）。プレースホルダ `REPLACE_WITH_*` を実名に置き換えた。カタログ名・資格情報は従来どおり環境変数からのみ読む。(2) `SPEC.md` §1.2 の「やらないこと」から Web UI を外し、§10.3 として CLI／ノートブック／UI の責務境界を定義した。(3) **`screener` を方式ごとに作り直した。** `infer` と `assume` は選択肢を提示しないのに `options` と `pass_if` を必須にしており、使い道の無い設定を書かせていた。`ask` は `questions:`、`assume` / `infer` は自然言語の `conditions:` だけで成立するようにし、方式に合わない書き方は移し方を添えて停止する。条件文の出口を `Screener.condition_texts()` に一本化し、`infer` の `screener_responses` は条件ごとではなく**判定1件＝1レコード**（予約 `question_id: _infer`）に改めた。あわせて、判定済み候補の判定に設問一覧を使っていた箇所（`infer` では空になり、`all()` が空集合に対して真になって誰も判定されない）を `expected_question_ids()` に置き換えた。(4) UI の設定を**2層**に分けた。`config/ui_config.yaml`（調査横断の固定設定）と画面入力を `persona_sim/uiconfig` が合成して調査定義を作り、`survey_from_dict()` に通す。UI 経由と調査定義ファイル経由で通る検証を同じにするため。割り付けは3パターンとも `quotas.mode: proportion` で比率を渡し、整数配分は既存の最大剰余法（`allocate_cell_sizes()`）に委ねる。セッション数も `validate_static()` の `Estimate` をそのまま使い、数え方を2箇所に持たない。国勢調査の参照テーブルは `persona_sim/data/` に置き場所と出典欄を用意した（実数値は未投入。無ければ当該パターンを選択不可にする）。(5) 仕様書を `docs/issues/concept_survey_ui_spec.md` から `docs/SPEC_UI.md` へ移して全面改訂し、非同期実行を Databricks Jobs API 方式と定め、走行中の進捗が現状取れない理由と `runs` に要る列を明記した。Streamlit アプリ本体・Jobs 投入コード・`runs` の列追加は後続。

### 12. コンセプト調査 Web UI アプリの実装
- **日付**: 2026/07/28 23:30
- **計画**: [docs/history/20260728-233000-web-ui-app-plan.md](docs/history/20260728-233000-web-ui-app-plan.md)
- **結果**: [docs/history/20260728-233000-web-ui-app-result.md](docs/history/20260728-233000-web-ui-app-result.md)
- **概要**: `docs/SPEC_UI.md` のうち設定層より先（読み取り経路・投入・画面）を実装。**`runs` の破壊的変更を含む**（既存テーブルは列追加が要る）。(1) CLI は Spark、UI は SQL Warehouse で行を読むが**同じ数字になる**必要があるため、集計の Spark 依存部に継ぎ目を入れた。`frame.answers_from_rows()` / `aggregate.build_result()` / `panel_composition_table_from_rows()` を切り出し、`aggregate_survey()` は「Spark で読む → `build_result()`」だけにした。振る舞いは変えておらず、既存の Spark テストが緑のままであることで確認している。(2) 調査の種類 `survey.type`（`SurveyType(StrEnum)`、既定 `concept`）を追加。既定値があるので既存の調査定義は壊れない。自由文字列にしないのは、綴り違いがそのまま記録に残ると種別ごとに集計できなくなるため。(3) `runs` に `survey_name` / `survey_type` を追加。一覧のたびに全件の `metadata_json` を掘るのは無駄が大きく、種別で絞ることもできない。(4) `persona_sim/storage/warehouse.py` を新設。クエリ組み立ては純関数、接続だけ薄い層にし、配列カラムは SQL 側で `to_json` して文字列で受け取る（コネクタの複合型の扱いが私的な接続オプションに依存するため、そこに挙動を預けない）。値は必ず名前付きパラメータで渡す。(5) `persona_sim/uiconfig/jobs.py` を新設。調査定義を Volumes に置き、事前定義ジョブを `run_now` で起動する。Streamlit は操作のたびに再実行されるので `idempotency_token` を必ず渡す。(6) `app/` に Streamlit アプリ（調査設計・結果閲覧の2ページ）。`pages/` の自動検出ではなく `st.navigation` を使い、ファイル名は英語・表示名は日本語という規約を両立させた。ページはウィジェットと表示だけで、ロジックは `persona_sim` 側に置いてある。(7) `deploy/README.md` にホイール作成・ジョブ構成・権限・疎通確認の手順。**実環境でのデプロイと疎通は未検証**で、SQL Warehouse への実接続・Volumes への書き込み・ジョブ投入は動作確認できていない。画面1 はローカルで起動して確認し、その過程で「UI の作る全調査に対処不能な警告が出る」問題（`W_PROMPT_TEMPLATE_VERSION`）を見つけて設定側で版を宣言するよう直した。

### 13. `template_version` の廃止とジョブ実行の notebook 化
- **日付**: 2026/07/29 14:00
- **計画**: [docs/history/20260729-140000-prompt-template-version-and-notebook-job-plan.md](docs/history/20260729-140000-prompt-template-version-and-notebook-job-plan.md)
- **結果**: [docs/history/20260729-140000-prompt-template-version-and-notebook-job-result.md](docs/history/20260729-140000-prompt-template-version-and-notebook-job-result.md)
- **概要**: PR #9 マージ後の確認で追加された2件のフォローアップ（`docs/issues/202607291302.md`）に対応。**調査定義の破壊的変更を含む**（既存 Delta テーブルの作り直しは不要）。(1) 生成AIの回答には再現性が無く、版で過去と比較する前提が成立しにくいうえ、利用者がプロンプトを自由に書き換える運用を想定しているため、`prompt.template_version` による版管理を全廃した。既存の廃止フィールドの機構（`REMOVED_MODEL_FIELDS` 等）にならい `REMOVED_PROMPT_FIELDS` を新設し、黙って無視せず廃止理由つきのエラーで読み込みを止める。版据え置きを検知していた `validate` の警告（`W_PROMPT_TEMPLATE_VERSION`）と専用ヘルパも削除し、`run_metadata.json` / `prompt_sample.md` / `config/ui_config.yaml` / `SPEC.md` / `quickstart.ipynb` から該当箇所を除去した。(2) アプリ構成（UI が調査定義→ジョブが実行→UI が結果確認）のうち、ジョブ側の実行を単一の `notebook_task`（`notebooks/run_survey.ipynb`）に一本化した。Databricks Jobs API の `job_parameters` はタスク種別に依存しないため `persona_sim/uiconfig/jobs.py` は無変更。新設したノートブックは `dbutils.widgets` で Volumes 上の調査定義パスを受け取り、`validate → panel → screen → run → aggregate`（export 含む）を順に実行し、失敗時は例外でジョブを失敗させる。`deploy/README.md` §3 と `docs/SPEC_UI.md` §6.1 をこの構成に更新した。

### 14. wheel の Python 要件を 3.11 に緩和
- **日付**: 2026/07/29 19:50
- **計画**: [docs/history/20260729-195000-lower-python-floor-to-3-11-plan.md](docs/history/20260729-195000-lower-python-floor-to-3-11-plan.md)
- **結果**: [docs/history/20260729-195000-lower-python-floor-to-3-11-result.md](docs/history/20260729-195000-lower-python-floor-to-3-11-result.md)
- **概要**: PR #10 マージ後、Databricks Apps へのデプロイで `persona-sim requires a different Python: 3.11.15 not in '>=3.12'` により失敗した。`pyproject.toml` の `requires-python = ">=3.12"` はリポジトリ立ち上げ時に `SPEC.md` §13（将来の API 実装向けの技術スタック表）に合わせて一律で設定したもので、Databricks Apps / ジョブが実際に動く Python 3.11 と食い違っていた。コードは 3.12 専用構文を使っておらず（`enum.StrEnum` は 3.11 から利用可能。3.11 の AST パーサで全ソースが構文エラー無く読めることを確認済み）実質的な非互換は無かったため、`requires-python` を `>=3.11` に緩め、ruff の `target-version` も `py311` に揃えた。乖離の再発を防ぐため CI（`.github/workflows/ci.yml`）のテスト対象 Python も 3.11 に揃え、`SPEC.md` §13・`README.md`・`notebooks/quickstart.ipynb` の記述も実態に合わせて修正した。`python -m build --wheel` でホイールの `Requires-Python` が `>=3.11` になることと、非Spark 391件・Spark 63件のテストが全件パスすることを確認済み。

### 15. `run_survey.ipynb` のアプリ非依存化と単体テスト対応
- **日付**: 2026/07/30 01:00
- **計画**: [docs/history/20260730-010000-run-survey-standalone-plan.md](docs/history/20260730-010000-run-survey-standalone-plan.md)
- **結果**: [docs/history/20260730-010000-run-survey-standalone-result.md](docs/history/20260730-010000-run-survey-standalone-result.md)
- **概要**: `notebooks/run_survey.ipynb` はこれまでアプリ（`app/`）の「調査を開始する」ボタン経由の `job_parameters={"survey": ...}` でしか起動されておらず、UI 側と notebook 側のテストが切り分けにくかった。調べると notebook 自体は `persona_sim.*` しか import しておらずアプリへの直接依存は無く、結合点は (1) `survey` ウィジェットが空のときのエラー文言が「アプリ側が渡すはずの」とアプリ限定を前提にしていた点、(2) `storage_config()` が `PERSONA_SIM_CATALOG`/`PERSONA_SIM_SCHEMA`/`PERSONA_SIM_WAREHOUSE` を環境変数のみから解決し、ジョブクラスタ側の事前設定に依存していた点だった。既存のアプリ→ジョブ投入経路は変えずに、(1) notebook の cell-0 にクラスタアタッチや Databricks Jobs UI の「今すぐ実行」でアプリを経由せず起動する手順を追記し、cell-1 のエラー文言を修正、(2) `catalog`/`schema`/`warehouse` の任意ウィジェットを追加してクラスタ未設定でも値を上書きできるようにし、(3) Volumes に置いて単体で素早く・無料（`model.endpoint: fake`・小規模）で試せる `examples/survey_sample_smoke.yaml` を新設、(4) `deploy/README.md` に「ノートブック単体の動作確認」節を追加、(5) `tests/test_run_survey_notebook.py` の新設と `tests/test_survey_loader.py` へのサンプル検証テスト追加を行った。Databricks 実環境での動作確認は環境が無いため未実施（`deploy/README.md` の既存の未検証注記と同様）。

### 16. 調査設計ページの性別別ターゲティング・コンセプト操作・ライトモード見直し
- **日付**: 2026/07/30 02:00
- **計画**: [docs/history/20260730-020000-ui-fixes-3-items-plan.md](docs/history/20260730-020000-ui-fixes-3-items-plan.md)
- **結果**: [docs/history/20260730-020000-ui-fixes-3-items-result.md](docs/history/20260730-020000-ui-fixes-3-items-result.md)
- **概要**: `docs/issues/UIの修正.md` の3件に対応。(1) job側（`persona_sim/panel`）はセル単位で性別・年齢を独立して持てる作りですでに対応済みと分かったため、UI (`app/views/survey_design.py`) と橋渡し層（`persona_sim/uiconfig/*`）だけを変更。単一の年齢スライダーを、性別ごとのチェックボックス＋from-to数値入力に置き換え、`SurveyForm.sex_ranges`（性別→年齢範囲のマッピング。キーに無い性別は対象外）として`build_quotas()`/`ratios()`/`build_survey_dict()`に通す。グローバル`panel.filters`は性別を1つしか選ばなければ従来どおり性別・年齢とも絞り、複数性別（範囲が違う場合を含む）を選んだときは性別条件を外し年齢だけ全範囲を覆うenvelopeにする（割り付けセル条件が性別・年齢を厳密に絞るため安全）。(2) コンセプトの追加・削除を、位置ベース（`concept_count`というintと`range()`）から安定ID方式（`concept_ids`のリスト＋使い回さない`next_concept_id`）に作り替え、各コンセプトに個別の削除ボタンを、一覧の下に追加ボタンを1つ配置した。(3) `app/.streamlit/config.toml`の`[theme]`（背景色・入力欄の下地色・アクセント色）と、`app/lib/layout.py`の`inject_css()`（角丸・薄枠線のみの最小限CSS）でライトモードの見た目を調整した。あわせて、`tests/test_uiconfig.py`の`CONFIG_PATH`が実際の設定ファイルの場所（`app/config/ui_config.yaml`）と食い違っていたバグ（PR #12で報告したベースブランチの既存障害と同一）を修正し、これに依存していた27件のテストのERRORを解消。Streamlitアプリを実際に起動しPlaywrightのブラウザ操作で、テーマ適用・性別除外時の`panel.quotas.cells`/`panel.filters`・コンセプトの追加/削除を確認済み。`./scripts/run-tests.sh`は403件全件パス。

### 17. `examples/survey_sample.yaml` を結合済みフォーマットにブラッシュアップ
- **日付**: 2026/07/30 03:20
- **計画**: [docs/history/20260730-032000-brush-up-survey-sample-plan.md](docs/history/20260730-032000-brush-up-survey-sample-plan.md)
- **結果**: [docs/history/20260730-032000-brush-up-survey-sample-result.md](docs/history/20260730-032000-brush-up-survey-sample-result.md)
- **概要**: `docs/issues/202607301208.md`（「survey volume の調査 yaml と `config/ui_config.yaml` の2つが notebook 入力になっており `model` などが重複設定」という指摘）を調査したところ、UI 経由の本番経路は `persona_sim/uiconfig/build.py` の `build_survey_dict()` がすでに `model`/`prompt` を含め結合済み1本の yaml を survey volume に保存しており、`notebooks/run_survey.ipynb`（2026-07-30 01:00 改修）も `survey` ウィジェット1つだけを読む単一入力になっていて、この指摘は既に解消済みだった。起票者に範囲を確認したところ、対象は UI を経由しない手動運用のサンプル `examples/survey_sample.yaml` で、`model:` を独立して手書きしていたため `config/ui_config.yaml` の `model:` と構造的に二重管理になっており（`max_tokens_open: 512` が漏れていた）、`prompt:` は丸ごと省略されて `persona_sim/panel/schema.py` の `PromptConfig` 組み込みデフォルトに暗黙依存していた（たまたま `config/ui_config.yaml` と同一内容だったため症状が出ていなかった）。`examples/survey_sample.yaml` の `model:` に `max_tokens_open` を補い、`prompt:` を `config/ui_config.yaml` の内容で明示的に書き切ることで「UI 入力 + `ui_config.yaml` の結合済みフォーマット」の見本として自己完結させ、冒頭コメントに同期が必要な旨を明記した。`examples/survey_sample_smoke.yaml` は smoke test 専用の値のまま維持し、`prompt:` を意図的に省略している理由をコメントで明示した。`./scripts/run-tests.sh tests/test_survey_loader.py -v` で37件全件パスを確認。

### 18. `ui_config.yaml` と調査定義を「スクリーニング→本調査」構造へ再編成
- **日付**: 2026/07/30 06:00
- **計画**: [docs/history/20260730-060000-config-restructure-plan.md](docs/history/20260730-060000-config-restructure-plan.md)
- **結果**: [docs/history/20260730-060000-config-restructure-result.md](docs/history/20260730-060000-config-restructure-result.md)
- **概要**: `docs/issues/202607301208.md` に追記された4件に対応。**調査定義と `config/ui_config.yaml` の破壊的変更を含む**（既存 Delta テーブルの作り直しは不要）。(1) 調査定義の最上位を `survey` / `panel` / `screening` / `main_survey` / `stimuli` / `design` / `questions` / `output` に再編成した。旧 `model:` / `prompt:` は `main_survey:` 配下へ、旧 `panel.screener:` は最上位 `screening:` へ移し、両ブロックを model → prompt → persona_card → その他の同じ並びに揃えた。`prompt:` が本調査（`system`）とスクリーニング（`infer_system`）を同居させていたのが再編成の直接の動機で、**同型の混在が `prompt.rules` にもあった**（設問タイプごとの回答指示文と、設問タイプではない判定指示行 `infer` が同じ block にあり、dataclass のコメント自身が「`for_type` からは引かない」と断っていた）ため `screening.prompt.rule` へ分離した。`headings` の前提ブロック見出しはスクリーニング由来の名前だが描画されるのは本調査のカード末尾なので `main_survey.prompt` に残した。(2) ペルソナカードを `PersonaCardConfig`（`attributes` / `include_summary` / `persona_fields`）として本調査・スクリーニング共通の概念にし、**属性行を属性ごとに制御できるようにした**。真偽値 `include_attributes` は `attributes: []` に包含されるため廃止（両方持つと矛盾しうる）。属性行の並びが `run/prompt.py` と `panel/infer.py` に二重にハードコードされていたので純関数 `attribute_row()` に寄せたが、**整形は共用していない**（判定カードは1人1行に詰める。回答生成用を流用すると `infer` の費用面の利点が消えるという既存の設計判断を保つ）。あわせて `personas/load.py` に `required_persona_columns()` を新設した。属性行が設定可能になると `FIXED_PERSONA_COLUMNS` では足りず、`region` などを指定してもエラーにならずその項目が抜けたカードで聞いてしまうため。`metadata.py` にもカードの実際の内容を残す（§9.1 の再現性）。(3) `ui_config.yaml` の最上位を `survey_defaults`（そのまま調査定義になるもの）と `ui`（画面を描くためだけのもの）の2群にした。起票時の分類とは3点変えており、`default_questions`（→ `questions`）・`survey_type`・`output.base_segments` はいずれも調査定義に入るので `survey_defaults` 側に置いた。(4) `output.formats` を `ui_config.yaml` から外した。結果画面は SQL Warehouse を直接引き、Excel / CSV のダウンロードは押されたその場で生成するため、ジョブが書き出したファイルを誰も読んでいない（調査定義のスキーマとしては残してあり CLI・ノートブックからは指定できる）。読み手が1箇所も無かった `screener.available_persona_fields` も削除した。(5) 旧形式は黙って無視せず移行先を添えて停止させ、移行の各パターンに1件ずつテストを対応させた。非 Spark テストは429件全件パス（新規26件）、Spark テストも pyspark を導入して63件全件パス。Streamlit アプリを起動して Playwright で調査設計ページを操作し、エラーなく描画されること・見積もりが新しい `UIConfig` から正しく引けていることを確認済み。Databricks 実環境での確認は未実施。

### 19. `examples/survey_sample.yaml` にスクリーナーを追加 / `ui_config.yaml` に定性フィールド一覧コメントを追記
- **日付**: 2026/07/30 07:00
- **計画**: [docs/history/20260730-070000-survey-sample-screener-and-persona-fields-comment-plan.md](docs/history/20260730-070000-survey-sample-screener-and-persona-fields-comment-plan.md)
- **結果**: [docs/history/20260730-070000-survey-sample-screener-and-persona-fields-comment-result.md](docs/history/20260730-070000-survey-sample-screener-and-persona-fields-comment-result.md)
- **概要**: 利用者からの指摘2件に対応。設定の実挙動を変える変更は無く、破壊的変更も無い。(1) `examples/survey_sample.yaml` は本番規模サンプルにもかかわらず `screening:` が無く、スクリーニングなしの調査定義になっていた。`app/config/ui_config.yaml` の `survey_defaults.screening`（`mode: "infer"`）の内容をそのまま書き写し、UI では画面入力になる `conditions` だけこのサンプルの題材（RTD アルコール飲料）に合わせた具体例（`examples/survey_sample_smoke.yaml` と同じ文言）を書いた。冒頭コメントも `screening:` が `ui_config.yaml` 由来である旨に更新した。(2) `main_survey.persona_card.persona_fields` に選べる定性フィールドの一覧が設定ファイル上から追えず、`personas_base`（`SPEC.md` §2.1）まで遡る必要があったため、`ui_config.yaml` の該当箇所に選択可能な全9項目（既定選択中の4項目には注記）を列挙するコメントを追加した。過去の履歴（#18）で読み手の無い `screener.available_persona_fields` を実フィールドとして削除した経緯があるため、今回は実行時に読まれるフィールドとしては追加せず、コメントに留めた。`./scripts/run-tests.sh` で非 Spark 429件全件パスを確認済み。

### 20. `screener.mode: infer` の判定プロンプトが効かない問題を修正
- **日付**: 2026/07/30 09:00
- **計画**: [docs/history/20260730-090000-screener-infer-fix-plan.md](docs/history/20260730-090000-screener-infer-fix-plan.md)
- **結果**: [docs/history/20260730-090000-screener-infer-fix-result.md](docs/history/20260730-090000-screener-infer-fix-result.md)
- **概要**: `docs/issues/screeningの問題.md`（判定プロンプトを選択型から除外型に書き換えても結果が1件も変わらず、通過率0.9%のまま E2 になる）を調査し、独立した3つの欠陥を修正した。**調査定義の破壊的変更を1点含む**（`screening.mode: infer` での `logic:` が書けなくなる）。(1) 主因は `persona_sim/panel/infer.py` の `build_judge_messages()` が、`screening.logic` から生成した「すべての条件を満たす人物を選んでください。」という設定不能な一文を `screening.prompt.rule` の**直前**に割り込ませていたこと。system prompt より近く rule より前という最も効く位置にあるため、`prompt` を除外型に書き換えても真逆の指示に引き戻されていた（`app/config/ui_config.yaml` は既に除外型に書き換わっており、同ファイルの `logic: "all"` が効いていた）。この一文を廃止して判定の指示を `prompt.rule` に一本化し、`infer` で何もしなくなった `logic` は書いたら停止するようにした（`ask` の `pass_if` 結合方法としては従来どおり有効）。(2) `screener_responses` が `(survey_id, persona_uuid, question_id)` で upsert されるだけで判定設定をキーに持たず、無効化する経路が一つも無かったため、同じ `survey_id` で回し直すとプロンプトやモデルを変えても古い判定がそのまま再利用されていた。`screening_fingerprint()`（mode / conditions / questions / batch_size / 判定モデル / prompt / persona_card の正規化 JSON の sha256）を新設し、`screener_responses` に `config_hash` 列として残して、現在の設定と一致する行だけを「判定済み」とみなすようにした。`oversample_factor` と `concurrency` は判定の中身を変えないので指紋に含めない（含めると通過者不足の再試行のたびに全件を聞き直すことになる）。列を持たない既存テーブルは全件未判定として扱い自己修復する。設定を変えずに引き直すための `persona-sim screen --force` も足した（初回のみ効き、同じ実行の再試行では既判定を再利用する）。(3) 通過率が構造的に低いとき `oversample_factor` を 4→8→16→32 と倍にしても通過率は変わらず必要数に届かないのに上限まで再試行しており、届かない判定の呼び出し費用だけが増えていた。純関数 `unreachable_cells()` を新設し、各試行の後に到達可能性（`通過率 × 必要数 × 上限倍率 >= 必要数`。必要数が相殺されて `通過率 × 上限倍率 >= 1` に落ちる）を確かめ、満たさないセルがあれば上限まで試さずに E2 で停止して必要な候補数の見積もりを添えるようにした（報告の事例では「必要な候補数の見積もり 約1067名（現在の通過率 0.9%）に対し、上限の 32 倍でも 320 名までしか増やせない」と出る）。なお**通過率0.9%そのものは実装の不具合ではなく方式の限界の可能性が高い**（判定カードに飲用頻度の手がかりが載っていない）ため、方式選択の見直しは論点として issue に残しスコープ外とした。あわせて、本変更前から失敗していたベースブランチの既存障害 `tests/test_uiconfig.py::test_built_survey_uses_the_screening_and_main_survey_blocks`（`batch_size` の期待値20を直書きしていたが `app/config/ui_config.yaml` は30）を、設定ファイル側を正として `ui.screening.batch_size` との一致を見る形に改めた（数値を直書きすると設定を変えるたびに本質と無関係な失敗になるため）。非 Spark テストは452件全件パス（新規23件）、Spark テストは pyspark 4.0.1 / delta-spark 4.3.1 を導入して70件全件パス（新規7件）。

### 21. Databricks Apps のソースをリポジトリ直下に移し、ホイールの作成を運用から外す
- **日付**: 2026/08/04 13:41
- **計画**: [docs/history/20260804-134146-app-source-root-drop-wheel-plan.md](docs/history/20260804-134146-app-source-root-drop-wheel-plan.md)
- **結果**: [docs/history/20260804-134146-app-source-root-drop-wheel-result.md](docs/history/20260804-134146-app-source-root-drop-wheel-result.md)
- **概要**: 「`persona_sim` を更新するたびにホイールを作り直すのが手間なので `app/` の中に移せないか」という相談から始めた。調べた結果、ホイールが要る原因は配置ではなく **Databricks Apps のソースコードパスの取り方**だった。Apps は指定したソースディレクトリ配下だけを同期し、そのルートの `requirements.txt` を install するため、ソースが `app/` だとその外にある `persona_sim/` に届かず、Volume 上のホイール（`app/requirements.txt` の最終行）で埋める必要があった。そこで **`persona_sim/` は動かさず、Apps のソースコードパスをリポジトリ直下に移した**（`app/app.yaml` → `app.yaml`、`command` を `['streamlit', 'run', 'app/app.py']` に）。import は `app/lib/__init__.py` が既に持っていた「`persona_sim` が見つからなければリポジトリ直下を `sys.path` に入れる」処理でそのまま通る。`persona_sim/` を `app/` 配下へ移す案は、`persona_sim` が CLI・ノートブック・ジョブから使う本体で `app/` はその上に乗る薄い UI であるという依存の向きが構造から読めなくなること、それでいてジョブ側のホイールは消えないことから採らなかった。あわせて `notebooks/run_survey.ipynb` にも `quickstart.ipynb` と同じ `sys.path` ブートストラップを入れ、ジョブ側（Git フォルダ内のノートブックとして登録する前提）からもホイールを外したので、ホイールを作る作業は運用から完全に無くなった。Apps がソースルートの `requirements.txt` を自動で install する都合上、ルートの `requirements.txt` はアプリ依存（pyspark を含まない）に置き換え、従来の実行基盤向け依存は `requirements-pipeline.txt` に分けた（CI の Spark ジョブと `README.md` の手順も追随）。作業ディレクトリがリポジトリ直下に変わるため、`app/lib/context.py` の `config/ui_config.yaml` を cwd 相対から `DEFAULT_UI_CONFIG_PATH` によるモジュール相対に直した。`pyproject.toml` は変更しておらず、`python -m build --wheel` も `pip install -e .` も従来どおり通る（運用で使わなくなっただけ）。検証は `ruff check .` パス、非 Spark テスト470件全件パス、cwd をリポジトリ直下にした状態で設定ファイルの解決先と `persona_sim` の解決先（インストール無しの環境でリポジトリ内のソースを指すこと）を確認、`streamlit run app/app.py` の起動で HTTP 200、`AppTest` によるアプリ本体の実行で例外・警告なしに調査設計ページの5セクションが描画されることを確認済み。**実環境（Databricks Apps / ジョブ）での疎通確認は未実施。**

### 22. `infer` の判定失敗が「非通過」として確定する問題を修正
- **日付**: 2026/08/05 04:08
- **計画**: [docs/history/20260805-040837-infer-judge-failure-plan.md](docs/history/20260805-040837-infer-judge-failure-plan.md)
- **結果**: [docs/history/20260805-040837-infer-judge-failure-result.md](docs/history/20260805-040837-infer-judge-failure-result.md)
- **概要**: `docs/issues/20260805001.md`（UI からの実行が1度目・2度目とも別々のエラーで落ちた）を調査した。添付の Databricks ノートブックエクスポート2点をデコードして各セルの状態を読んだ結果、**2つのエラーは同じ1つの原因の症状**で、表示されていた理由はどちらも事実と違っていた。根は「`infer` 判定の LLM 呼び出しが全バッチ失敗したこと」1点。(1) 1度目は全バッチ `LLMError` → `_infer_execution` が `aborted_reason` を立てるが、その前に `screener_responses` へ行を書いて `return` するためパネルは `candidate` のまま。`aborted_reason` を `persona_sim/cli.py` は見ているのに `notebooks/run_survey.ipynb` の screen セルが見ておらず、中断が握りつぶされて次のセルが `role=main のパネルが1件も無い。先に screen を実行すること` という無関係な文言で落ちていた。(2) 2度目は、1度目に書かれた行が現在の `config_hash` を持つ「判定済み・非通過」として `read_screener_codes` に読み戻され、`todo` が空になって**LLM を1回も呼ばないまま**全員 `screened_out` に確定し、`実インシデンス 0.0% / 通過者が0名のため候補を何倍に増やしても届かない` で E2 停止していた。つまり**通信の失敗が「この人たちは条件に合わなかった」という判定結果として永続化され、再実行しても二度と聞き直されない**状態で、`assume` のインシデンスを 1.0 と記録しないのと同じ性質の誤りが判定失敗側で起きていた。対処として、スクリーナー専用フラグ `judge_error` を新設して呼び出し失敗の行に立て（`parse_error`＝返ってきたが番号が取れなかった、とは別物）、`read_screener_codes` がその行を返さないようにした。行自体は `answer_raw` にエラー文を持つ証跡として残り、再実行で聞き直されて upsert で上書きされる。あわせて screen セルが判定数を表示して `aborted_reason` があれば例外を送出するようにし、中断理由に実際のエンドポイントエラー文を載せた（従来は「エンドポイントの設定を確認すること」だけで、原因は Delta テーブルを掘るまで読めなかった）。**呼び出しがなぜ失敗したかは未確定**で、修正版の再実行でエラー文が出てから判定エンドポイント側を切り分ける必要がある（Databricks 実環境での確認は未実施）。

### 23. 結果閲覧ページをクロス集計表だけにし、ダウンロードを1ファイルに集約
- **日付**: 2026/08/05 06:00
- **計画**: [docs/history/20260805-060051-results-crosstab-only-plan.md](docs/history/20260805-060051-results-crosstab-only-plan.md)
- **結果**: [docs/history/20260805-060051-results-crosstab-only-result.md](docs/history/20260805-060051-results-crosstab-only-result.md)
- **概要**: `docs/issues/20260805002.md`（結果閲覧ページをクロス集計表だけにしたい／ダウンロードは1ボタン／ローデータが読めない）に対応した。起票文書自体が貼り付け事故で1〜7行目重複・4行目が途中で切れて別の文に接続していたため、まず重複を除去して意図を復元し、確認結果を追記した。要求の1つ目「軸＝コンセプト、セグメント＝全体で n・各選択肢・T2B・平均」は既存のどの表でも作れなかった（`crosstab_table()` は表側＝セグメント、`concept_summary_table()` は表側＝コンセプトだが各選択肢の比率を持たない）ため、`tables.py` に `concept_axis_table()` を追加した。ただし**集計はやり直していない**――材料は `compute_metric()` を通った `Crosstab` の値で、並べ替えと整形しかせず、`crosstab_table()` と値が一致することをテストで固定した。実装中に、`group_by_segments()` が回答からセグメント値を作る都合で**回答が1件も無いコンセプトは表から消える**ことが分かったので、n=0 の行として残し注記で名指しするようにした（出したのに出てこないのか出していないのかが読み分けられなくなるため）。画面はコンセプト比較（Plotly グラフ・サマリーカード）を削除し、属性別クロス集計の選択肢を `crosstab_*` に限定した（従来は `concept_summary` / `panel_composition` が混ざっていた）。ダウンロードは `st.download_button` が1ファイルしか返せないため **xlsx 1ファイル・シート2枚**（`集計表` / `ローデータ`）に同梱する `write_ui_workbook()` を新設した。CLI と共用の `write_xlsx()` は触っていない（UI の都合で `report.xlsx` の中身が変わるのを避けるため）。「シート1枚」にすると概要シートの E3・E4 注記が消えるので、集計表シートの**表頭より上**に注記行として残し、位置もテストで固定した。設問ごとに選択肢ラベルが違うため選択肢列は番号だけに揃え、ラベルは同じく表の上の凡例に出す。ローデータには `rawdata.py`（新規）で `stimulus_name` と `answer_labels` を足した。**ラベルを引く前に `to_defined_codes()` で定義順へ戻している**――`answer_codes` は提示順なので、飛ばすとシャッフルした設問で別の選択肢の名前が付く。あわせて `responses_query()` を `r.*` から明示列＋`to_json` に直した（配列カラムを `to_json` して受け取るのは `warehouse.py` 自身の方針で、このクエリだけ従っておらずコネクタの挙動に預けていた）。グラフを外したことで `app/lib/charts.py` が未参照になり plotly も不要になったため、両方削除して `docs/SPEC_UI.md` §4.3・§4.4・§6.3 を実際の画面に合わせた。検証は `ruff check` パス、非 Spark テスト496件全件パス（変更前473件、23件追加）。**Databricks Apps 実環境での画面確認は未実施。**

### 24. run_survey ノートブックにトークン使用量の表示を追加
- **日付**: 2026/08/05 06:58
- **計画**: [docs/history/20260805-065850-notebook-token-usage-plan.md](docs/history/20260805-065850-notebook-token-usage-plan.md)
- **結果**: [docs/history/20260805-065850-notebook-token-usage-result.md](docs/history/20260805-065850-notebook-token-usage-result.md)
- **概要**: `docs/issues/20260805003.md`（run_survey ノートブックの最後にスクリーナー・回答生成・合計のトークンを出したい）に対応した。単価はワークスペースごとに違うためシステム側は金額換算を持たない方針（`SPEC.md` §9、`metadata.py` の `estimated_cost: None`）で、費用を把握する唯一の手掛かりがトークン数なのに、本番ジョブの出力には出ていなかった。調べたところ材料は半分しか揃っておらず、回答生成側は `RunResult.input_tokens` / `output_tokens` があって CLI も表示していた一方、**スクリーナー側は `ScreeningResult` にフィールドが無く、行としては `screener_responses` にあるのに結果オブジェクトが集計していなかった**。そこで `ScreeningResult` にトークンを足し、`screen_survey()` がセッション数を足し込む箇所で一緒に加算した（**中断の early return より前**に置いたので、判定が全滅して中断した場合も呼んだぶんは計上される。オーバーサンプルの再試行で複数回通るため `+=`）。ここで `mode: infer` に落とし穴があり、**1バッチ＝1呼び出しなのに `_to_record()` が候補1人につき1行を作って全行に同じバッチのトークン数を載せている**ため、回答生成側と同じ「レコード単純合算」を持ち込むと1回しか呼んでいないのに `batch_size` 倍（設定値では30倍）の数字が出る。`InferResult` にトークンを持たせて `run_inference()` のバッチ受け取り箇所で足す形にし、テストで固定した（副次的に、`screener_responses` テーブルの `input_tokens` 列は `infer` 行では人数ぶん重複していることが分かったが、バッチを識別する列が無く保存形式の変更は移行が要るため、テーブルを読まない設計にして回避しスコープ外とした）。`_InferExecution` は `ExecutionResult` と同じ属性名で持たせ、呼び出し側は `ask` と `infer` を1本の経路で扱えるまま保った。あわせて `ExecutionResult` に合算プロパティを足し、`run/run.py` の `_summarize()` の同じ式をそこへ寄せた（挙動不変）。集計範囲は**この実行で呼び出したぶんだけ**とし（`run_metadata.json` の `tokens_scope: "run"` と同じ）、再開でスキップしたセッションと判定済みで聞き直さなかった候補は含まない。調査全体の累計を出す案は上記の重複により `infer` のスクリーナー行を正しく合算できないため採らなかった。ノートブックには末尾に markdown + code の2セルを追加し、スクリーナー／回答生成／合計を入力・出力の2列で出す。日本語ラベルは等幅表示で2桁を占め桁数指定では列がずれるので、`unicodedata.east_asian_width` で表示桁を数えて揃えた。CLI（`persona-sim screen`）の表示と `run_metadata.json` への追記は起票の範囲外として行っていない。検証は `ruff check` パス、非 Spark テスト499件全件パス（変更前496件、3件追加）、Spark テストは `test_screening_spark.py` / `test_run_spark.py` の48件全件パス（新規2件）。**Databricks 実環境での確認は未実施。**

### 25. 結果閲覧ページのダウンロード失敗を修正し、一覧の表示を整理・高速化
- **日付**: 2026/08/05 09:13
- **計画**: [docs/history/20260805-091312-results-download-and-list-plan.md](docs/history/20260805-091312-results-download-and-list-plan.md)
- **結果**: [docs/history/20260805-091312-results-download-and-list-result.md](docs/history/20260805-091312-results-download-and-list-result.md)
- **概要**: `docs/issues/20260805004.md` の3件（ダウンロードが `TypeError: Excel does not support timezones in datetimes` で落ちる／結果閲覧ページの「実行セッション数」「成功」「失敗」「完了日時」は読み手に意味が伝わらないので出したくない／調査一覧が出るまで待たされる）に対応した。1件目の原因はローデータに載せていた `responses.ts` で、`ts timestamp` 列を databricks-sql-connector が**タイムゾーン付きの `datetime`** で返すため、openpyxl がセルに書けずこの1列があるだけで `workbook.save()` が落ち、集計表シートごとダウンロードが失敗していた（集計表シート側の値は数値と文字列だけなので、原因はローデータシートに限られる）。tz を落とす／JST に換算する／ISO 文字列にする案も出したうえで**列ごと外す**判断とし、`RAW_RESPONSE_COLUMNS` から `ts` を削除した。`export.py` に「datetime が来たら tz を外す」汎用ガードは入れていない――どのタイムゾーンで出すかを決めないままラベル無しの UTC を書く状態に戻るため。CLI の `responses_raw.csv` は `frame.stream_responses_raw()`（Spark 経由）の別経路で CSV はタイムゾーンを扱えるので触っていない。2件目はメトリクス4枚を削除し、消費側がいなくなった `RUN_COLUMNS` を `survey_id` / `survey_name` / `finished_at` / `aborted_reason` の4列に絞った。**中断の警告（`aborted_reason`）と調査選択プルダウンの表示名は残した**――前者はデータが欠けているサインで黙って消すと不完全な集計を完全なものとして読んでしまい、後者の完了時刻と `survey_id` は同名の別実行を見分けるためのもの（`docs/SPEC_UI.md` §4.1）で、不要だったのはメトリクス行だけと解釈した。3件目は `fetch_runs()` にキャッシュが無いのが原因で、**Streamlit がウィジェット操作のたびにページ全体を再実行する**ため調査を選び直すだけで毎回ウェアハウスへ問い合わせが飛んでいた。`app/lib/context.py` に `runs()` を足して `@st.cache_data(ttl=60)` を掛け（接続と設定はハッシュできないので `_` 始まりの引数名でキーから外し、`survey_type` だけをキーにする）、`show_spinner` で初回に何を待っているかを出すようにした。更新ボタンは起票の「最初に見えるのは選択と取得ボタンのみ」を崩すため足さず、TTL で代替している。あわせて副次的に見つけた不整合として、`runs_query()` に `QUALIFY ROW_NUMBER() OVER (PARTITION BY survey_id ORDER BY finished_at DESC) = 1` を足して `survey_id` ごとに最新1件へ畳んだ――畳まないと画面が `survey_id` をキーに辞書へ入れ直すところで後勝ちの上書きが起き、新しい順に並べているぶん**最古の行**が残って、一覧に出る完了時刻が実際に取得するデータ（調査定義は最新1件から復元する）と食い違っていた。検証は `ruff check` パス、非 Spark テスト505件全件パス（変更前499件、6件追加。うち4件は変更前のコードで落ちることを確認済み）。**Databricks Apps 実環境での確認は未実施。**

### 26. パネル抽出の再計算爆発を解消（セル数に対して指数的 → 線形）
- **日付**: 2026/08/06 03:07
- **計画**: [docs/history/20260806-030737-panel-selection-performance-plan.md](docs/history/20260806-030737-panel-selection-performance-plan.md)
- **結果**: [docs/history/20260806-030737-panel-selection-performance-result.md](docs/history/20260806-030737-panel-selection-performance-result.md)
- **概要**: Databricks で 200サンプルの調査を実行したところ `build_panel` が13分を超えても終わらず、Spark UI では単一ジョブが15ステージ・2700タスクを抱え、ステージのタスク数が `36 → 63 → 99 → 144 → … → 603`（差分が 9 ずつ増える三角数、合計 2655）と並んでいた。原因は `select_members` のセルループで、除外用の `selected` が「これまでの全セルの `picked`」の union であり、`picked_k` 自身も `selected_{k-1}` との anti-join を含むため、k番目のセルの計算木に 1〜k-1番目の計算が入れ子で入っていたこと。コードには「各セルの結果は cache 済みなので再計算は起きない」というコメントがあったが、`persona_sim/` 全体に `cache()` / `persist()` / `checkpoint()` は**1箇所も無く**、前提が崩れたままコメントだけが残っていた。さらに `picked.count()` がセルごとに action を打つため、この再計算がセル数だけ繰り返されていた。**コストを決めていたのはサンプル数ではなくセル数**で、ステージが10本だったのは割り付けが `性別 × 10歳刻み`（男女 × 5バンド = 10セル）だったため。対処として、(1) 母集団を抽出条件が使う8列（`quotas.SELECTION_COLUMNS` として定数化。`personas_base` は26列あり長文の生成列を含む）に射影して1回だけ `cache()` し、`try` / `finally` で必ず `unpersist()` する、(2) `picked.count()` を `picked.collect()` に替えて確定行を driver 側に取り出し、anti-join の除外集合を前セルの DataFrame ではなく確定済み uuid のリストから `spark.createDataFrame` で作り直す、(3) `parts` の union 連鎖を廃して最後に一度だけ `members` を組む（`finalize_panel` が既に使っているパターン。パネルは高々 `panel.size × oversample_factor` の数百行）——の3点を行った。**セル内の抽出ロジック（`_rank_key` の `sha2`・`orderBy(...).limit(needed)`・`row_number().over(Window.orderBy(...)) - 1`）は一切変えていない。** セル条件から一括で振り分ける（when チェーン）書き換えは、重複除外が「先に**選ばれた人**を除外」から「条件に**合う人**を除外」に変わり `SPEC.md` §5.0 の意味が変わるため採らなかった。効果は実測で、`select_members` のステージ数がセル数 2/4/6/8 に対して修正前 12/32/106/396 → 修正後 12/18/24/30（`3 × セル数 + 6` の線形）。修正前は二次ですらなくセル2つごとに約3.5倍の**指数的**な増え方で、8セルで約13分の1になった（Spark UI の三角数は単一ジョブ内で同一 Exchange が再利用されて二次に抑えられていた分で、セルごとの action をまたぐと再利用が効かず指数的に伸びていた）。出力が変わっていないことは、重なるセルを含む10セル・同一 seed で修正前後の `select_members` を突き合わせ、`(persona_uuid, cell_id, cell_rank, role)` 78行と `achieved` / `raw_candidates` / `overlap_excluded`（250件）/ `weights` が完全一致することで確認した。回帰テストは、この症状が既存の正しさのテスト（seed再現性・並列度非依存・重複なし・round-robin 均等）を全部通過してしまうため、`setJobGroup` ＋ `statusTracker` でステージ数を数えて 2セルと6セルを比較する `test_selection_cost_grows_linearly_with_cells` を新設し、修正前のコードで落ちることを確認した。あわせて `SELECTION_COLUMNS` の取りこぼしを検出する `test_selection_columns_cover_every_filter_field` を追加。検証は `ruff check` パス、非 Spark テスト505件全件パス、Spark テスト全件パス（新規2件）。`screening.py` がリトライで `build_panel` を最大4回呼ぶ構造は候補を増やすための設計なのでそのままとし、`Window.orderBy` に `partitionBy` が無いことによる `No Partition Defined for Window operation` 警告は対象が `limit` 後の数百行で実害が無く `cell_rank` の決め方が変わりうるため触っていない。**Databricks 実環境での実測は未実施。**

### 27. run の進捗表示と、エンドポイント再試行の可視化
- **日付**: 2026/08/06 07:15
- **計画**: [docs/history/20260806-071500-run-progress-and-retry-visibility-plan.md](docs/history/20260806-071500-run-progress-and-retry-visibility-plan.md)
- **結果**: [docs/history/20260806-071500-run-progress-and-retry-visibility-result.md](docs/history/20260806-071500-run-progress-and-retry-visibility-result.md)
- **概要**: Databricks で `run_survey()` が数十分〜1時間以上かかるのに実行中の出力が一切無く、遅いのか止まったのか判別できない、という報告から着手した。切り分けの過程で、本番ジョブの入口である `notebooks/run_survey.ipynb` だけが `progress=` を渡していないこと、スクリーナーの実測が `avg_attempt=1.0 / p50 2,853ms / p95 22,902ms` と**パース再試行では説明できない8倍の裾**を持つこと、実行中に同じエンドポイントへ外から投げると 20,667ms（平常時 1,034ms）かかることが分かった。ところが `DatabricksServingClient` が 429/5xx を最大5回・指数バックオフで握って再試行しているぶんは**どこにも記録されておらず**、`Completion.latency_ms` はバックオフ込みで計測され `responses.attempt` はパース失敗の回数なので、「モデルが遅い」のか「429 で眠っていた」のかを実行後に切り分けられない状態だった。今回は**観測できるようにすることだけ**を行い、実行の挙動（並列度・再試行回数・バックオフの計算）は変えていない。(1) 進捗コールバックの引数の形が `execute()` 系＝`ExecutionResult`、`infer` 系＝整数 `1` の2種類あり、CLI の `_progress` が前者しか受け取れないため **`persona-sim screen` を `mode: infer`（`ui_config.yaml` の既定）で実行すると `AttributeError` で落ちていた**ので、`persona_sim/run/progress.py` に `ProgressUpdate(done, total, failed)` を新設して両者を揃えた（変更前のコードで落ちることをテストで確認済み）。(2) 同モジュールの `console_reporter()` に表示を集約し、**間引きを「50件ごと」から「30秒ごと」へ変えた**——1セッション20秒級では件数間引きだと更新が5分に1回になり生存確認にならないため。完了行は間引かず必ず出す（最後の1行が無いと完走したのか止まったのか分からない）。残り時間は見積もりではなく**実測スループット**から出し、`isatty()` が真のときだけ `\r` で上書き、Databricks のセル出力では改行して積む。(3) `llm/client.py` に `RetryStats` と `SupportsRetryStats` を追加し、`DatabricksServingClient` にロック付きカウンタを持たせた。計上は `finally` で行うので**全滅した呼び出し**（＝最も再試行が多い）も数え落とさない。`LLMClient` 本体には足していない——`complete` だけを持つスタブが tests に5つあり必須メソッドにすると巻き込むため、`isinstance` で判定する。理由キーは HTTP ステータス（429 と 503 は打つ手が違うのでまとめない）。(4) `RunResult` と `ScreeningResult` の両方に載せ、5%（E3・E4 と同じ閾値）超で `W_ENDPOINT_RETRY` を警告に積み、`run_metadata.json` の `model.endpoint_retries` に残す。警告は `_summarize()` の**早期 return より前**に置いた（1件も書き出せなかった実行で消えると最も知りたい場面で何も残らない）。`ScreeningResult.warnings` はフィールドだけあって誰も書き込んでいなかったので今回が最初の書き手で、CLI の screen 経路にも印字を足した。`screen_survey()` は戻り口が3箇所あるため `_finish()` に切り出して全経路で同じものを載せている。(5) ノートブックは run セルで `build_client(survey.model)` を作って `run_survey` と表示で共有し（共有しないと進捗行に再試行を出せない。モデルは従来どおり調査定義から引く）、screen セルにも進捗を渡し、**両セルで `warnings` を印字するようにした**（従来は印字しておらず `W_STRUCTURED_OUTPUT` も画面に出ていなかった）。`SPEC.md` は §9 の JSON 例と §11 の注記を同時に更新した。検証は `ruff check` パス、非 Spark テスト536件全件パス（変更前505件、31件追加）、Spark テスト81件全件パス（変更前76件、5件追加）。**`concurrency` の調整とエンドポイント側の割り当て変更、呼び出しごとのタイムアウト、`responses` の中間コミットは今回のスコープ外**（まず記録を取ってから判断する）。**Databricks 実環境での確認は未実施。**

### 28. 出力長超過（`max_tokens` 到達）のリトライ対応
- **日付**: 2026/08/07 07:19
- **計画**: [docs/history/20260807-071924-output-limit-retry-plan.md](docs/history/20260807-071924-output-limit-retry-plan.md)
- **結果**: [docs/history/20260807-071924-output-limit-retry-result.md](docs/history/20260807-071924-output-limit-retry-result.md)
- **概要**: `docs/issues/20260807001.md`（3600セッション中4セッションが `Could not finish the message because max_tokens or model output limit was reached` で失敗）に対応した。エンドポイントが HTTP 400 を返し、`_is_retryable()` が 429/5xx しか再試行対象にしないため即座に `LLMError` になり、`_ask` → `run_session` を素通りしてセッション失敗として計上されていた。**失敗したセッションは1行も書き出さない**ので、そのペルソナが既に答え終えていた設問の回答まで丸ごと消える——欠けたのは4回答ではなく4セッション分の全設問だった。ただし3596セッションは同じ `max_tokens` で通っており、**これは設定不足による決定論的な失敗ではなく、出力長が非決定的に揺れて稀に予算を超える分布の裾**である。つまり分類が事実と合っていない（400 は本来「何度投げても同じ」を意味するが、この 400 は枠を広げた再送に意味がある）。`_ask` は既にパース失敗に対して「同じ入力を再送する。揺れを作るのはエンドポイント側の非決定性だけ」という理屈で再送しているのに、同じ非決定性が原因の出力長超過だけが1回で諦めていた。対処は、(1) `OutputLimitExceeded` を `LLMError` の下に新設して `RetryableLLMError` と分ける（待てば直るものと枠を広げるべきものは打つ手が違う）、(2) `llm/budget.py` を新設し `max_tokens` を 4倍・16倍・天井4096 で引き上げて再送する（送信そのものは呼び出し側の closure に委ね、モジュールは予算の決め方だけを持つ——`_ask` が構造化出力のフォールバックを内側に持つため、クライアントを渡すと2つの方針が絡む）、(3) 天井まで広げても通らなければ**セッションを殺さず** `output_limit` フラグを立てて記録し続行する（§6.3 の「3回失敗したら `parse_error` を付けて続行」と同じ型）、の3点。判定順は**構造化出力より前**に置いた——誤分類のコストが非対称で、出力長超過を `StructuredOutputUnsupported` と取り違えると `StructuredOutputState` が実行全体で共有される1個であるため**調査まるごとが正規表現パースに落ちる**（逆向きは1呼び出しで済む）。併せて、`finish_reason` をリポジトリのどこでも読んでいなかった穴も塞いだ——今回のエンドポイントは 400 を返したが、200 + `finish_reason: "length"` で途中までの本文を返す挙動なら**切り詰められた回答がフラグなしで `answer_raw` に流れる**。検査は `content is None` チェックより前に置いた（推論だけで予算を使い切った応答は本文が空で返るので、後に置くと「message.content が空」と原因を取り違える）。フラグは**広げて通った回答には立てない**——`retried` を `QUALITY_FLAGS` から外す論拠「リトライは経緯であって最終的に得られた回答の質ではない」がそのまま当てはまるため。引き上げが起きた事実は `run_metadata.json` の `model.output_limit` と `[W_OUTPUT_LIMIT]` 警告に残す（残さないと「予算が足りていない」という兆候がどこにも現れない）。引き上げは**パース試行を消費しない**（`responses.attempt` の意味を保つ）、使い切ったら**パースループを短絡する**（しないと1設問あたりの呼び出しが3倍に膨れる）。`mode: infer` では `complete_within_budget` を `except LLMError` の**内側**で呼んでいる——`OutputLimitExceeded` は `LLMError` のサブクラスなので、直に `client.complete` を囲んだままだと引き上げが働く前に握りつぶされ1バッチまるごと未判定になる。使い切った場合は現行どおり `judge_error` として残し（再実行で聞き直される良い劣化の仕方なので変えていない）、`error` 文言に直し方を入れた。検証は `ruff check` パス、非 Spark テスト565件全件パス（変更前538件、27件追加）、Spark テスト81件全件パス。**裾の真因である `model.thinking` が payload に載っていない問題（`docs/issues/202607281100.md`）は未対応で、予算超過の発生頻度そのものはそちらを直すまで下がらない**（今回の変更は起きたときに失われないようにするだけ）。抑止パラメータ名と受け付けられる値は実機確認が要る。`config/ui_config.yaml` の `max_tokens` は引き上げ再送が自己修復するため変更していない。**Databricks 実環境での確認は未実施。**

### 29. コードベース全体レビュー（所見の洗い出し）
- **日付**: 2026/08/07 13:36
- **計画**: [docs/history/20260807-133614-codebase-review-plan.md](docs/history/20260807-133614-codebase-review-plan.md)
- **結果**: [docs/history/20260807-133614-codebase-review-result.md](docs/history/20260807-133614-codebase-review-result.md)
- **所見**: [docs/issues/20260807002.md](docs/issues/20260807002.md)
- **概要**: 「コード全体を見てバグ、改善点を洗い出してほしい」という依頼で `persona_sim/` 全モジュール・`app/`・`scripts/`・`hooks/`・CI 定義を通読し、**25件**（当リポジトリ22件 ＋ retrospective 3件）の所見を `docs/issues/20260807002.md` に記録した。**読み取り専用のレビューでコードは1行も変更していない**（修正は `/pdca` に引き継ぐ）。前提として `AGENTS.md` の実装上の不変条件（thinking OFF・順序尺度をシャッフルしない・`screened_out` を消さない・`assume` のインシデンスを 1.0 と記録しない・`infer` の通過率を実測欄に入れない・`disjoint` のセル内 round-robin・確定後の `cell_rank` 振り直し・品質フラグは立てるだけで除外しない）は**いずれも実装側で守られている**ことを確認している。高（結果の意味が変わる／実行が壊れる）は5件。(H1) `__version__ = "0.1.0"` と `pyproject.toml` の `0.3.0` が食い違い、`run_metadata.json` の `persona_sim_version` と `--version` が実装を指していない（`.venv` 上で実測して所見に添付。§9.1 の「各バージョンが揃えば入力が再現できる」の根拠が崩れる）。(H2) `presentation: simultaneous` × `memory: none` × `rotation: random` で**設問1だけがペルソナごとの提示順、設問2以降は全員同じ定義順**になる——`_unit_groups()` はグループ先頭のユニットにしか `stimuli_to_present` を持たせず、`memory: none` では `build_sessions()` が各ユニットを独立セッションに割るため2問目以降は必ず `_fallback_stimuli()` を通り、その `ALL_STIMULI` 分岐が `ctx.stimuli`（＝調査定義順）を返している。`responses.options_order` は選択肢の順序しか残さないので**提示順のずれはどのテーブルにも残らず**事後に気づけない（UI 経由は `sequential` 固定なので、YAML を直接書く CLI／ノートブック経路で `rotation: random` を選んだ場合に限る）。(H3) `infer` 判定で `LLMError` 以外の例外が出ると `run_inference()` の裸の `future.result()` から伝播し、`screen_survey()` が `merge_upsert` する前に抜けるので**完了済みバッチの判定結果もまとめて失われる**（同じ立場の `executor._collect()` は `except Exception` で吸収しており、`judge_batch()` の docstring も「1バッチの失敗で調査全体を止めない」と書いているので意図と実装が食い違っている）。(H4) SQL Warehouse 接続を `@st.cache_resource` で持っており、これは利用者ごとに分かれないため全 Streamlit セッションで1本の接続を共有している。`databricks-sql-connector` の Connection は複数スレッドから同時に使えず、2人が同時に「データを取得」を押すと**片方の結果がもう片方に返りうる**（`results.py` が `st.caption(f"survey_id: ...")` を出してまで防ごうとしている事故そのもの）。(H5) ジョブ投入の冪等トークンが `f"persona-sim-{survey_id}"` で時刻もセッションも含まないため、docstring の言う「二度押し防止」ではなく**「同じ `survey_id` は約64日間、二度と実行できない」**が成立している。`run_now` は既存 run の ID を返すので**成功と区別がつかず**、失敗した実行の投入し直し（最も踏みやすい経路）で無言に空振りする。中は8件で、straightline 判定が `multi` を含むのに `answer_codes[0]` しか見ていない（コメントは「single/scale は1要素」と書いており、同じ判定が `flags.straightline_personas` に未使用のまま二重実装されている）／`_looks_like_output_limit()` が「400系 かつ 文言に `max_tokens`」だけで判定するため `max_tokens must be <= N` のような**設定不正まで拾って予算を4倍・16倍に広げて投げ直し**、最終的に「`max_tokens` を見直すこと」と**下げるべき場面で上げるよう案内**する／推論リクエストにタイムアウトが無く、E5 は「失敗が返ってくること」を前提にしているのでハングした場合は進捗表示ごと無言で止まる（`docs/history/20260806-071500-*` が意図的に先送りした項目で、記録は既に揃っている）／E5 中断時に `cancel()` できなかった実行中セッションの結果を捨てている（最大 `concurrency` 件の呼び出しが課金だけされて消え、再実行で二重に払う）／`finalize_panel()` が `by_cell` でループするため候補ゼロのセルが `shortfalls` に載らず `complete` が真になる（`screen_survey()` が E1 を握りつぶす `population_capped` 経路から到達しうる）／`crosstabs()` と `concept_summary()` が表ごとに `answers` を全走査するので Web UI の取得時間に直に乗る／`infer` で `sessions_skipped` がペルソナ数・`sessions_total` がバッチ数なのに CLI が同じ「バッチ」ラベルで並べ、内訳が総数を超えて見える／`SPEC.md` §15.1 が出力への添付を義務づける `OUTPUT_DISCLAIMER` が Web UI のフッタにしか無く、**実際に配られる `report.xlsx`・CSV・CLI 出力に入っていない**。低は9件（proportion 合計が1超のときスライスが反転して誤配分する／到達しないコード8シンボルと、loader と validate は扱うのにプロンプトにも集計にも効かない `Question.scale_points`／既定値のリテラル二重持ち／ロール名の直書き／Web UI が操作のたびに xlsx を再生成／`WorkspaceClient` 遅延生成のロック欠如／細かいもの4件／`app.yaml` のカタログ名直書き／型チェッカ不在）。retrospective 側は3件（SessionStart フックの cwd 依存・`link-repo.sh` の冪等性・`git pull` のタイムアウト欠如）で、`docs/issues/` の仕組みが当リポジトリにしかないため記録だけこちらに寄せ、直すのは向こう側と明記した。なお `app.yaml` のカタログ名直書きは当初「問題無し」に分類していたが、`AGENTS.md:93` の「カタログ名・スキーマ名は書かない」と、エンドポイント名だけをカーブアウトした `CHANGELOG` 項目9 の「カタログ名・資格情報は従来どおり環境変数からのみ読む」に照らして整合しないため **L8 として所見に格上げした**（バグではなく解釈のずれなので、ルール側／実態側どちらを直すかは判断材料だけ書いて決めていない）。H2 の修正方針も2案を併記して決めていない。検証は `ruff check` パス、非 Spark テスト565件全件パス（コード未変更）、`git diff --stat` が `docs/` と `CHANGELOG.md` のみであることを確認。**Spark テストは Java の無い環境のため未実行**（コード未変更なので影響は無いが、`/pdca` で M5・M6 の修正に入る際は必要）。**H4・H5 は実機確認ができておらず**、API 仕様とコードの読みからの指摘である。

### 30. コードベースレビューの高優先度所見（H1〜H4）の修正
- **日付**: 2026/08/07 14:15
- **計画**: [docs/history/20260807-141500-high-severity-fixes-plan.md](docs/history/20260807-141500-high-severity-fixes-plan.md)
- **結果**: [docs/history/20260807-141500-high-severity-fixes-result.md](docs/history/20260807-141500-high-severity-fixes-result.md)
- **所見**: [docs/issues/20260807002.md](docs/issues/20260807002.md)
- **概要**: 項目29のレビューで**高**に分類した所見を修正した。当初 H1〜H5 の5件を予定していたが、**着手時に H5 の重大度判定が誤っていたことが判明したため取り下げ**、実際の修正は H1〜H4 の4件（H5 の経緯は末尾）。(H1) `__version__` のリテラル `"0.1.0"` を消し `importlib.metadata.version("persona-sim")` から引くようにして、`pyproject.toml` を版の SSoT にした。`0.3.0` と食い違ったまま `run_metadata.json` の `persona_sim_version` と `--version` に流れており、`SPEC.md` §9.1 の「seed と各バージョンが揃えば入力が再現できる」の根拠が崩れていた（**0.1.0 と記録された実行が3つあっても、それぞれ別のコードかもしれない**）。あわせて docstring の「現在の実装範囲は M1 と M2」も実態（M1〜M7 ＋ Web UI）に直した。版とセットで古くなっていた記述で、放置すると同じ誤読を招く。(H2) `presentation: simultaneous` × `memory: none` × `rotation: random`／`balanced` で、**設問1だけがペルソナごとの提示順、設問2以降は全員同じ定義順**になっていた。`_unit_groups()` が同時提示のグループで先頭ユニットにしか `stimuli_to_present` を持たせず、`memory: none` では `build_sessions()` が各ユニットを独立セッションに割るため2問目以降が必ず `_fallback_stimuli()` を通り、その `ALL_STIMULI` 分岐が `ctx.stimuli`（＝調査定義順）を返していた。`responses.options_order` は選択肢の順序しか残さないので**提示順のずれはどのテーブルにも残らず**、`run_metadata.json` の `prompt_sample` も1名の先頭設問だけなので事後に気づく手段が無い。対処は「提示順を全ユニットに持たせ、推測で埋める経路そのものを無くす」方針を採り、`_unit_groups()` に `keeps_history = survey.design.memory is not Memory.NONE` を持たせて `index == 0 or not keeps_history` で入れる形にした（同時提示・逐次提示の両分岐に同じ条件）。`_fallback_stimuli()` の `ALL_STIMULI` 分岐は**削除**し、到達すれば `KeyError` になるようにしてある——推測で定義順を返して黙って結果を変えるより落ちるほうがよい、という判断。なぜ補わないかは docstring に残した。所見には `rotation: random` としか書いていなかったが `balanced` でも起きる（`assignment_table()` が `cell_rank` ごとに巡回窓を作る）ので所見側に追記した。UI 経由は `presentation: sequential` 固定なので、YAML を直接書く CLI／ノートブック経路のみが対象。(H3) `infer` 判定で `LLMError` **以外**の例外（`personas[uuid]` の `KeyError` 等）が出ると `run_inference()` の裸の `future.result()` から伝播し、`screen_survey()` が `merge_upsert` する前に抜けるため**完了済みバッチの判定結果もまとめて失われて**いた（同じ立場の `executor._collect()` は `except Exception` で吸収しており、`judge_batch()` の docstring も「1バッチの失敗で調査全体を止めない」と書いているので意図と実装が食い違っていた）。`_result_or_failure(group, future)` を切り出して `InferBatchResult(error=...)` に落とすようにし、既存の `judge_error` 経路（`batches_failed` に計上 → `codes` に入れない → `read_screener_codes()` が未判定として除く → 再実行で聞き直す）にそのまま乗せた。(H4) SQL Warehouse 接続を `@st.cache_resource` で持っており、これは利用者ごとに分かれないため**全 Streamlit セッションで1本の接続を共有**していた。`databricks-sql-connector` の Connection は複数スレッドから同時に使えず、Streamlit は接続中のブラウザセッションごとに別スレッドでスクリプトを再実行するので、2人が同時に「データを取得」を押すとリクエストが混ざる（壊れ方は運次第で、例外になるか**片方の結果がもう片方に返る**——`results.py` が `st.caption(f"survey_id: ...")` を出してまで防ごうとしている事故そのもの）。`app/lib/context.py` に `_QUERY_LOCK` と `query(fetcher, *args, **kwargs)` を足して取得を直列化し、`fetch_survey` / `build_result` / `fetch_responses_raw` / `fetch_runs` をすべてこれ経由にした。取得は「データを取得」押下時の1回だけなので体感は変わらない。副次的に `runs()` の `_connection` 引数と `results.py` の `connection` 変数が不要になったので削除した。`persona_sim/storage/warehouse.py` は**変更していない**——CLI・ノートブックは単一スレッドで、アプリ側の都合をパイプラインに漏らさないため。**H5（冪等トークン）は取り下げた。** 修正に着手した段でコードを追い直したところ、`submit_survey()` の呼び出し元は1箇所だけで渡る `survey_id` は必ず `{slug}_{時刻}_{secrets.token_hex(3)}` ＝投入ごとに一意（成功時に session_state から捨てられる）であり、トークンは実質「投入1回につき1つ」で docstring の言う二度押し防止は**実際に成立している**と分かった。所見に書いた「同じ `survey_id` は64日間実行できない／失敗した投入し直しで確実に踏む」は誤りだった。さらに当初案の「nonce を混ぜる」修正は、nonce の寿命を誤ると**今効いている二度押し防止を壊す**。残るリスクは「呼び出し側が `SurveyForm.survey_id` を固定した場合に2回目が無言で空振りする」という狭い罠だけなので、利用者に確認のうえ**挙動は変えず**、`idempotency_token()` の docstring に前提（`survey_id` が投入ごとに一意であることに依存している／ID を固定する経路を足すときは併せて見直す）を明記し、所見を低（L10）へ移して当初の記述の何が誤りだったかを書いた。検証は `ruff check` パス、非 Spark テスト**573件全件パス**（変更前565件、8件追加）。新規テストのうち5件が**修正前のコードで落ちること**を、対象ファイルを一時的に戻して確認済み。H1 は `.venv` 上で `persona-sim --version` が `0.3.0` を出すことを実測した。テストの書き方では、H2 は Spark を使わずに済ませるため定義順と違う並び（`["c3","c1","c2"]`）のパネル行を直接渡し、H3 は `_PERSONAS` が u1/u2 しか持たず `KeyError` が先に出てしまうため `_PERSONAS_4` を足して `RuntimeError` の経路を試し（`KeyError` 自体は別テストで押さえた）、H4 はこの環境に streamlit が入っておらず `app/` を import できないため**ソースの形で固定する**方式にした。**Spark テストは Java の無い環境のため未実行**（Spark 経路は変更していない）。**H4 の同時アクセスと Databricks Apps 実環境での確認は未実施**（Streamlit のランタイムを模さないと意味のある並行テストにならない）。中（M1〜M8）・低（L1〜L10）・retrospective（R1〜R3）は未着手で `/pdca` に引き継ぐ。L8（`app.yaml` のカタログ名直書き）は `AGENTS.md` 側と `app.yaml` 側のどちらを直すかの判断待ち。

### 31. コードベースレビューの中優先度所見（M1〜M8）の修正
- **日付**: 2026/08/07 15:20
- **計画**: [docs/history/20260807-152000-medium-severity-fixes-plan.md](docs/history/20260807-152000-medium-severity-fixes-plan.md)
- **結果**: [docs/history/20260807-152000-medium-severity-fixes-result.md](docs/history/20260807-152000-medium-severity-fixes-result.md)
- **所見**: [docs/issues/20260807002.md](docs/issues/20260807002.md)
- **概要**: 項目29のレビューで**中**に分類した8件を対応した。(M1) straightline の判定対象に `multi` が入っているのに `answer_codes[0]` しか見ておらず、`[1,2,3]` と `[1,5]` が「どちらも 1」として同一視されて**違う回答をした人にフラグが立って**いた。判定対象の決め方を `flags.straightline_question_ids()` に切り出して single / scale だけを返すようにした（複数選択を「同一位置を選んだ」に潰せないのは `crosstab._mean_score()` が平均を出さないのと同じ理由）。純関数に切り出したのは**テストのため**でもある——判定本体は Spark でしか動かずこの環境では実行できないが、対象の選び方はこれで非 Spark に固定できる。`STRAIGHTLINE` は `QUALITY_FLAGS` に入っているので、これは集計表の「フラグ除外後の n」に直接効いていた。(M2) `_looks_like_output_limit()` が「400系 かつ 文言に `max_tokens`」だけで判定するため `max_tokens must be <= 4096` のような**値の不正**まで拾い、`complete_within_budget` が4倍・16倍と**さらに大きい値**で投げ直して（必ず失敗する）1呼び出しが3倍に膨らんだうえ、最終的に「`model.max_tokens` を見直すこと」と**下げるべき場面で上げるよう**案内していた。手がかりを打ち切りを示す語（`reached` / `output limit` / `max output` / `truncat`）に絞り、`must be` / `invalid` / `expected` / `out of range` / `not allowed` を含むものは除外した。主経路が `finish_reason == "length"` である旨も docstring に明記。(M3) **所見の記述が事実として誤っていた。** 「タイムアウトが無い」「無期限に占有」「無言で停止」と書いたが、`databricks-sdk` の `_BaseClient` は既定で `http_timeout_seconds=60` / `retry_timeout_seconds=300` を適用しており、無期限に詰まることは無い（`ApiClient.do()` に `timeout` 引数が無く `Config` 経由でしか設定できないことも確認）。実際の問題は**再試行の層が2つ重なっている**ことで、外側の `_invoke_with_backoff`（5回）が「1回失敗した」と数えるものは内側の SDK 再試行（300秒）で既に投げ直された後の結果であり、最悪 `5 × 300秒 ≒ 25分`を1回の `complete()` に費やす。しかも `RetryStats` は外側しか数えず、**`llm/budget.py` は「再試行の軸は4つ」と列挙しているのに SDK 分が書かれていない**（軸を混ぜるなと戒めている当のファイルが5つ目を見落としていた）。利用者の判断により**挙動は変えず**、(a) `ModelConfig.request_timeout_sec`（既定60＝SDK と同値）を追加して `Config(http_timeout_seconds=...)` まで通し、何秒で切れるのかを調査定義と `run_metadata.json` から読めるようにする、(b) `budget.py` の軸を5つに直す、(c) `databricks.py` に多重化と `RetryStats` の範囲を明記する、の3点にとどめた。**`retry_timeout_seconds` は触っていない**——小さくすると、こちらの `_RETRYABLE_ERROR_NAMES` が `ConnectionError` / `TimeoutError` を含まないため**今 SDK が吸収している接続リセットが致命傷になる**。ほどくなら `_is_retryable()` の拡張とセットで行う必要があり別サイクルに回した。(M4) E5 中断時に `cancel()` できなかった実行中セッションの結果を捨てており、最大 `concurrency` 件の呼び出しが課金だけされて記録が残らず（`shutdown(wait=True)` の裏で走り切る）、再実行で二重に払っていた。`cancel()` が偽を返した future を `wait()` で待って `_collect()` するようにした（`aborted_reason` は最初の理由を保つので中断理由は上書きされない）。(M5) `finalize_panel()` が候補のあるセルしか回らず、候補ゼロのセルが `shortfalls` に載らないまま `complete` が真になる形だった。**所見では「E1 を握りつぶす経路から到達しうる」と書いたがこれも誤りで**、`select_members()` が全セルで必要数を保証してから `panels` を書くので到達経路は無い（防御的な修正として実施し、所見の表現を訂正した）。割り付けにあるセルを必ず1周するようにしたうえで、**割り付けに無いセルの候補も落とさない**よう `requested ∪ by_cell` を回している——`requested` だけにすると調査定義を変えた後に残った候補行が書き戻されずに消えるため。`_shortfall_message()` はこの修正で `achieved` が全セルを含むようになり自動的に正しくなるので変更していない。(M6) `crosstabs()` と `concept_summary()` が表ごと・セグメント値ごとに `answers` を全走査しており、Web UI の「データを取得」の待ち時間に直に乗っていた。`_by_stimulus()` / `_by_question()` の索引引きに置き換えた（**挙動不変のリファクタリング**なので新規テストは足さず、既存の集計テストが同じ数字を出し続けることで担保）。(M7) `infer` で `sessions_skipped` が人数・`sessions_total` がバッチ数で、CLI が両方を「バッチ」というラベルで並べるため「1バッチ中 380 スキップ」と**内訳が総数を超えて**見えた。換算を `screening.skipped_session_count()` に切り出し、`infer` は `infer_session_count()` でバッチ換算、`ask` は従来どおり `人数 × 設問数`。M1 と同じく Spark を要さずに中身を固定するための純関数化でもある。(M8) `SPEC.md` §15.1 が出力への添付を義務づける `OUTPUT_DISCLAIMER` が Web UI のフッタにしか無く、**実際に配られる** CLI 出力・`report.xlsx`・UI のダウンロードには帰属表示しか付いていなかった。`persona_sim.attribution_text()` を追加して帰属表示（§15.3）と免責を**1つにまとめて**運ぶようにし（別々に運ぶと片方だけ付いた出力ができる、というのが今回の事象そのもの）、CLI 標準出力5箇所・`report.xlsx` の「概要」・`write_ui_workbook`・`run_metadata.json` に載せた。CSV は表そのものなので載せていない。検証は `ruff check` パス、非 Spark テスト**590件全件パス**（変更前573件、17件追加）。新規テストが**修正前のコードで落ちること**を、対象ファイルを一時的に戻して確認済み（M1 / M7 は import エラー、M2 / M4 / M8 は assert 失敗）。M3 は既定60・上書き120 がクライアントまで伝わることを、M8 は `attribution_text()` が両方を含むことを実測した。**Spark テストはこの環境に Java が無いため未実行**で、M1 の判定本体・M5 の新規テスト・M7 の実経路は **CI の `spark-test` ジョブでのみ確認される**。低（L1〜L10）・retrospective（R1〜R3）は未着手で `/pdca` に引き継ぐ（L8 は判断待ち）。**Databricks 実環境での確認は未実施。**

### 32. コードベースレビューの低優先度所見（L1〜L8）の修正
- **日付**: 2026/08/08 10:15
- **計画**: [docs/history/20260808-101500-low-severity-fixes-plan.md](docs/history/20260808-101500-low-severity-fixes-plan.md)
- **結果**: [docs/history/20260808-101500-low-severity-fixes-result.md](docs/history/20260808-101500-low-severity-fixes-result.md)
- **所見**: [docs/issues/20260807002.md](docs/issues/20260807002.md)
- **概要**: 項目29のレビューで**低**に分類した10件のうち、L1・L3〜L8 を対応し、L2 は判断の下りた範囲まで進めた。**破壊的変更が1件ある。(L2) `questions[].scale_points` を受け付けなくした**——読み込みも検証（5 / 7 / 10 のいずれか）もしていたのにプロンプトにも集計にも一切効いておらず（選択肢数は `options` の長さから取っている）、「設定したつもり」の記録だけが残っていた。使う先を作るのではなく廃止を選んだ理由は、尺度の点数を決めているのが `options` の数そのもので繋ぐ先が無いため。`REMOVED_QUESTION_FIELDS` を新設し `REMOVED_MODEL_FIELDS` / `REMOVED_PROMPT_FIELDS` と同じ作法で理由と移行方法つきに止める。**既存の調査定義に書いてあれば読み込みで停止する**が、調査定義・`examples/`・`SPEC*.md`・ノートブックのどこにも書かれていないことを確認済み（5段階にしたいなら `options` を5つ並べればよい）。あわせて参照ゼロの5シンボル（`flags.straightline_personas` / `flags.flag_rates()` / `errors.DesignMismatchError` / `schema.PersonaFilter.is_empty()` / `screening.SCREENER_COLUMNS`）を削除した。`run/prompt.py::follow_up_message` と `InferResult.codes` / `ScreeningVerdict.codes` は**残した**——production からの参照は無いが、テストが判定結果の中身を検査する足場に使っており、消すとテストごと失われる。(L1) `allocate_cell_sizes()` で比率合計が1を超えると `remaining` が負になり、`order[:-n]` が「末尾 n 個を除く全部」を返して「余りを配る」処理が**「余分に配る」処理に反転**していた（`validate` は合計1.0 を止めるが、`warehouse.fetch_survey()` は `validate_static()` を通さないので到達しうる）。(L3) 既定値がリテラルと dataclass で二重定義だったものを `ModelConfig()` / `ScreeningConfig()` からの参照に揃えた。**M3 の修正で自分が作り込んだ二重定義**（`DEFAULT_REQUEST_TIMEOUT_SEC` が `llm/databricks.py` と `panel/schema.py` の両方にあった）も同時に解消した。(L4) `metadata.py` のロール名リテラルを `ROLE_CANDIDATE` に。(L5) Web UI がウィジェット操作のたびにローデータ全件の xlsx を作り直していたので `@st.cache_data` を掛けた `context.workbook_bytes()` に包んだ。返すのは**バイト列でパスではない**——一時ディレクトリは関数を抜けた時点で消えるため、パスをキャッシュすると2回目以降に読めなくなる。(L6) `WorkspaceClient` の遅延生成を double-checked locking にした（錠は `_stats_lock` と兼ねず別に持つ）。(L7) `_check_unique_ids()` を `Counter` で1回走査に／`cli._print_table()` の `zip(strict=True)` は正しいので残し**生成側**（`aggregate/tables.py` が返す全 `Table` で行長＝列数）にテストを置き／全角幅の計算を `persona_sim/textwidth.py::display_width()` に集約して `cli.py` の私的コピーと `progress.py` の `ljust` を差し替え（進捗行の `\r` 上書きに消え残りが出ていた）／`_try_json_object()` の貪欲な正規表現を `json.JSONDecoder().raw_decode()` に置き換えた（非貪欲 `.*?` は入れ子の `{}` を途中で切るので採らなかった）。(L8) `app.yaml` のカタログ名直書きは**ルールを実態に合わせる**判断となり、`AGENTS.md` の「カタログ名・スキーマ名は書かない」を「**コードに**書かない」と限定したうえで、サービングエンドポイント名と同じ形の例外条項（デプロイ設定に配置先を書くのは可。どのカタログに書いたのかが読み取れないと結果を辿れないため）を追記した。`app.yaml` の挙動は変えていない。検証は `ruff check` パス・非 Spark テスト597件全件パス（新規7件を含む）・`examples/*.yaml` が引き続き読めることを実測。新規テストのうち L1・L2・L7-4 は**修正前のコードで落ちることを確認済み**（L3 の既定値テストは値がずれたときにだけ落ちる網で、同じ値の二重定義そのものは検出できない旨を所見にも明記した）。Spark テストは Java の無い環境のため手元では実行できないので CI に委ね、**`b70fcef` で 82件全件パス**（非 Spark ジョブとあわせて全ジョブ green）を確認した。**未対応**は L9（型チェッカ導入）・L10（冪等トークン）・R1〜R3（retrospective 側）と、M3 で持ち越した多重再試行の解消。

### 33. M3 の多重再試行をほどく（SDK に寄せて自前を薄く）
- **日付**: 2026/08/08 11:30
- **計画**: [docs/history/20260808-113000-m3-retry-unwind-plan.md](docs/history/20260808-113000-m3-retry-unwind-plan.md)
- **結果**: [docs/history/20260808-113000-m3-retry-unwind-result.md](docs/history/20260808-113000-m3-retry-unwind-result.md)
- **所見**: [docs/issues/20260807002.md](docs/issues/20260807002.md)
- **概要**: 項目31で「別サイクルに回す」とした M3 の再試行の層をほどいた。**あわせて項目31の記述を訂正している——「両者は掛け算になる」「最悪 `5 × 300秒 ≒ 25分`」は誤りだった**（M3 の訂正はこれで2度目。1度目は「タイムアウトが無い」）。`databricks-sdk` の `_base_client.py` / `retries.py` / `errors/customizer.py` を読み直すと、2つの層は重なっておらず**失敗の種類で担当が分かれていた**——429 / 503 は SDK 既定の `_RetryAfterCustomizer` が必ず `retry_after_secs` を付け `retried()` が必ず投げ直すので**生の 429 は自前の層に出てこない**、500 / 502 / 504 は逆に SDK が投げ直さないので自前の層だけが担当する、という住み分けになっていた。SDK が予算を使い切ると `TimeoutError("Timed out after 0:05:00") from 元の例外` を投げるが、この名前は `_RETRYABLE_ERROR_NAMES` に無く `status_code` も持たないため `_is_retryable()` が偽を返して打ち切られており、掛け算になりようがなかった。**残っていた実害は別のところにある。** (1) 429 の嵐が300秒続くと、レート制限だと読み取れない `TimeoutError: Timed out after 0:05:00` という文言の `LLMError`（＝再試行しても無駄）に化け、`concurrency: 32` なら32スレッドが各300秒を捨てて E5（連続20失敗）に至る。(2) `_reason_key()` が `TimeoutError` を記録するので 429 だったことが `run_metadata.json` の `endpoint_retries.reasons` に残らない。(3) 自前の層は 5xx 専用なのに5回・最大30秒のバックオフを持っており過剰だった。利用者の判断は**「SDK に寄せて自前を薄く」＋「試行回数を減らす」**で、`_unwrap_sdk_retry_timeout()` を新設して SDK が包んだ `TimeoutError` を `__cause__` から**1段だけ**開いてから分類するようにし（理由キーも例外の文面も元のステータスで残る）、開いた＝ SDK が予算を使い切ったものは**自前で投げ直さず**（二重に待つだけ通る見込みが無い）**`RetryableLLMError`** にして記録に残るコードを実態に合わせた。レート制限だった場合は文面に「`model.concurrency` を下げるか、エンドポイント側の割り当てを確認すること」を添える——**`W_ENDPOINT_RETRY` は 429 を数えられないので、ここが唯一の伝達経路になる**。`max_attempts` は 5 → **3**（担当が 5xx だけなら3回で足り、早く失敗させたほうが E5 に早く届いて詰まったまま課金され続ける時間が短い。設定には出さない）。`_RETRYABLE_ERROR_NAMES` の 429 系は**残した**——SDK 経由では届かないが、`workspace_client` を注入する経路では来るし、将来 SDK が 429 を customizer から外したときに消していると**無言で1回で捨てる**側に倒れるため、安全側に残す旨をコメントにした。`retry_timeout_seconds` は今回も触っていない（SDK の `Retry-After` 尊重は自前の指数バックオフより正確で、下げても `retried()` は諦める前に必ず1回眠る実装なので得が無い）。**一本化を採らなかったので 429・503・接続エラーの再試行は `RetryStats` に載らないまま**残る。これは受け入れたうえでの穴なので、`llm/client.py` の `RetryStats` と `retry_warning()` の docstring に**「0 は詰まっていないの意味ではない」**と明記し、`llm/budget.py` の「先頭2つは入れ子・待ち時間は掛け算」も「担当が分かれている・掛け算にならない」に直した。検証は `ruff check` パス・非 Spark テスト602件全件パス（新規5件）。新規テストのうち4件は**修正前のコードで落ちることを確認済み**（残る1件は過剰な unwrap を防ぐ網なので修正前でも通る）。**SDK の挙動はインストール済みのソースを読んで確認したものであり、実エンドポイントに 429 を出させた実測ではない。**

### 34. 調査官の記憶を設問ごとに保持する（設問の提示スロットへの紐づけ）
- **日付**: 2026/08/08 14:00
- **計画**: [docs/history/20260808-140000-question-level-memory-plan.md](docs/history/20260808-140000-question-level-memory-plan.md)
- **結果**: [docs/history/20260808-140000-question-level-memory-result.md](docs/history/20260808-140000-question-level-memory-result.md)
- **概要**: 調査官（ペルソナ）の記憶が `design.memory` の3値（`none` / `within_stimulus` / `full_session`）でしか指定できず**調査全体に一括で掛かる**ため、「購入意向の回答を踏まえて新規性を聞くが、次のコンセプト提示にはその記憶を持ち込まない」といった設問単位の要求が表現できなかった。保持の単位を**設問**に下ろし、`questions[].remember`（`none` / `all` / 設問IDのリスト）で指定できるようにした。**破壊的変更が2件ある。(1) `questions` は全コンセプトに繰り返すテンプレートではなくなった**——各設問が `slot`（そのペルソナが何番目に見るコンセプトについて聞くか、1始まり）に紐づき、評価するコンセプト数ぶん並べる。`remember` は設問IDで参照するので、**IDが実行時にも一意でなければ**「どの `q_intent` か」が決まらないため。`slot` はコンセプトIDではなく提示順の位置なので `rotation` / `sample_overlap` と直交する（実行時に `assigned_stimuli[slot - 1]` を引く）。`slot` の無い旧定義は m > 1 のとき**ローダーで停止**する——既定 1 で黙って読むと2つ目以降のコンセプトが聞かれずに消え、同じ調査定義で結果の意味が変わる。判定を `validate` でなくローダーに置いたのは、`run` が `validate` を通らずに実行できるため。m == 1 なら旧形式と展開が一致するのでそのまま読める（`survey_sample_smoke.yaml` は無改修で通る）。**(2) IDを一意にした代償で、コンセプト比較表を束ねる手段が消える**ので `measure`（省略時は `id`）を追加し、集計層の束ねキーを `question_id` から `measure` に移した。`Crosstab.question_id` → `Crosstab.measure`、`aggregates` テーブルの `question_id` 列 → `measure`（派生テーブルなので `persona-sim aggregate` の再実行で作り直せる。**`responses` は無変更**で、回答生成のやり直しは不要）。畳まないと「コンセプトAの購入意向」がAを1番目に見た人と2番目に見た人で別の指標に割れる。同じ `measure` の `type` / `options` 不一致は E6。**セッションの粒度は記憶から決まる**ようになった——「設問 A が設問 B を覚えている」なら B の回答が出てからでないと A を聞けないので同じセッションに入る。この連結成分（union-find）が実行単位であり再開単位で、3値は特殊形として**分割が従来と完全に一致する**ことをテストで固定した（`none` は設問ごと・`within_stimulus` は slot ごと・`full_session` は丸ごと）。記憶を持たない設問は依存を持たないので単独セッションになり、既定モードの並列度は落ちない。**プロンプトは毎ターン組み直す**方式（`replayed_messages()`）に変えた。`messages` を伸ばし続ける作りでは「直前までの全部」しか表現できず飛ばし越しが書けない。取っておくのは回答の文面だけでよく、設問文も選択肢の提示順も純関数から組み直せる。守る不変条件は**プロフィールは先頭の [user] に1回だけ**（回答済みターンの [user] を使い回すと、単独セッションで実行された設問の再生で2回出る）と**コンセプトはその列で初出のターンにだけ挟む**の2つ。あわせて `Unit.stimuli_to_present` を全 unit が常に自分のコンセプトを持つ形にし、`session.py` 自身のコメントが警告していた「提示順が2問目以降だけ定義順に化ける」を条件分岐でなく構造で潰した。`remember` は**列挙どおりで推移しない**（Q4→Q3、Q3→Q1 でも Q4 は Q1 を見ない）——プロンプトに何が載るかを調査定義から直読できるようにするため。再生は書いた順でなく聞いた順（`slot` 昇順・記述順）。検証まわりは、`slot` の範囲外と `1..m` の抜け（**提示されるのに1問も聞かれないコンセプト**）・`remember` の未知ID / 自己参照 / 前方参照を E6 にし、`W_ROTATION` の判定を `design.memory is NONE` から `retains_memory(survey)` に変えた（`memory: none` でも設問側に書けば順序効果は発生しうる）。セッション数の見積を `size * m * 設問数` から `size * len(session_groups(survey))` に変え、実行と同じ関数から数えるようにした（記憶を持たせてもコスト見積がずれない。20人 × 3案 × 3問で `remember` あり60・なし180 を実測）。見積の「設問数」は展開後の延べ数になるので**別々の問いの種類数を併記**する。`run_metadata.json` に解決後の `questions_memory` を追加した（`design.memory` は既定の略記でしかなく、何を持たせて聞いたのかがそこからは読めない）。UI は `uiconfig/build.py` が設問を slot 展開して書き出すので画面と入力は不変。検証は `ruff check` パス・**非 Spark テスト628件全件パス**（変更前602件、26件追加）。**Spark テストは82件全件パス**（Java 17 + pyspark 4.0.1 / delta-spark 4.3.1 をこの環境に導入して実行。前サイクルまでは Java が無く CI に委ねていた）だが、**一発では通っていない**。1回目は 20 failed / 10 errors で、Spark 側のひな形がコンセプト数を減らすのに設問は3 slot のままだったのが原因（`base_survey_dict(slots=n)` で揃えて解消）。2回目は 6 failed で、すべて設問IDの直書きと集計の measure 化への追随漏れ（`test_aggregates_match_the_raw_responses` は、c1 への回答を数えるのに slot 違いの設問すべてを見る必要があることをテスト側に突きつけた）。3回目で全件パス。**この失敗が実装の欠陥を1つ暴いた**——`validate` は範囲外の `slot` を E6 で止めるが `run` は `validate` を通らずに実行できるため、素通りすると `assigned[slot - 1]` が素の `IndexError` になり、パネル全員ぶんのセッションが「list index out of range」で落ちるだけで調査定義のどこが悪いか読めなかった。`stimulus_for()` で範囲を確かめ設問IDと件数を添えた `SurveyDefinitionError` にした。記憶を持たない設問の入力が従来と**1バイトも変わらない**ことを `replayed_messages()` と `initial_messages()` の同値比較で固定し、飛ばし越し・プロフィール1回・コンセプト初出のみ提示は送信メッセージ列を控えて実測した。**Databricks 実環境での確認は未実施。** **ついでに `examples/survey_sample.yaml` の既存の不整合も直した**——本変更以前から `validate` が `割り付けの合計 20 が panel.size 600 と一致しない` で止まっていた（`git stash` して確認済み）。サンプルは `load_survey()` が通ることしかテストしておらず検証まで見ていなかったため残っていたもので、割り付けを 100 × 6セル = 600 に直し、酒類カテゴリなので `filters.age_min: 20` も足した（`survey_sample_smoke.yaml` と揃えた）。`test_sample_yaml_is_valid` を `validate_static` まで通すよう広げて再発を防いでいる。記憶を持つ設問と持たない設問が混ざる場合の投入順（prefix cache）は決めておらず、並べ替えずパネル順で流す旨をコメントに残した。設問単位の再開と UI からの記憶指定は未着手。

### 35. `remember` の単純化と、実装レビューで見つけた欠陥4件の修正
- **日付**: 2026/08/08 16:00
- **計画**: [docs/history/20260808-140000-question-level-memory-plan.md](docs/history/20260808-140000-question-level-memory-plan.md)
- **結果**: [docs/history/20260808-140000-question-level-memory-result.md](docs/history/20260808-140000-question-level-memory-result.md)（同文書の「追記」節）
- **概要**: 項目34を実装後に読み直したところ、**テストは全件通っているのに黙って結果が変わる欠陥が4件**見つかった（すべて実測で再現）。根っこは2つで、`remember` の意味を別の設定に依存させたことと、`run` が `validate` を通らないこと。**(1) `remember: all` の範囲が未定義のまま「コンセプトまたぎ」に倒れていた。** `all` の範囲を `design.memory` から決める作りにしていたが、その既定値（`none`）のときに範囲が定義されず、実装はまたぐ側に倒れていた。案3の回答に案1・案2の評価が混ざり、**反実仮想モナディックの前提（案どうしが独立）が壊れる**のに `validate` は「問題なし」と言う。利用者の判断で**根っこから単純化した**——`all` は「それまで全部」ただ1つの意味（コンセプトをまたぐ）、「同じコンセプトの中だけ」は設問IDを並べて書く（専用の値は足さない。コンセプト調査は数ある調査の一種でしかなく、その都合を `remember` に持ち込むと同じ語が文脈で別のものを指す）、そして**`remember` は `design.memory` の影響を一切受けない**。その帰結として `design.memory` はやることが無くなったので**廃止した**（残したまま無視すると「設定したつもり」の記録だけが残る）。`§5` の「3軸」は **2軸（誰が何を見るか × どう見せるか）＋ 設問ごとの記憶**になり、「覚えているか」は調査全体ではなく設問の性質になった。移行は `REMOVED_DESIGN_FIELDS` に案内つきで載せている（`none` → 何も書かない／`full_session` → 全設問に `remember: all`／`within_stimulus` → 各設問に同じコンセプトの先行設問IDを並べる）。**(2) 同じ `measure` の `top_box` 不一致が検査されていなかった。** `type` と `options` は検査していたのに `top_box` を見ておらず、集計は measure ごとに**代表1つの設問から T2B を出す**ので、コンセプトごとに「上位いくつ」がずれていても表は出てしまい**全コンセプトが1つ目の定義で計算される**（`is_ordinal()` も `top_box` を見るので平均の有無も入れ替わる）。**(3)(4) 設問が無言で消える2経路。** `run` は `validate` を通らずに実行できる（`cli.py` は各サブコマンドで `load_survey()` を呼ぶだけ）ため、**設問IDの重複**（実行計画が設問IDをキーにした対応表なので畳まれ、6問定義しても5問しか聞かれない）と**`slot` の抜け**（そのコンセプトは評価枠を消費するのに1問も聞かれず、集計表に n=0 の行だけが残る）が素通りしていた。`survey_from_dict()` は `SurveyDefinition` を組み立てる唯一の場所で CLI・ノートブック・画面が全部ここを通るので、**結果の意味が変わる不備はここで止める**（`_reject_unusable_questions()`）。項目34で Spark テストに踏まされた slot 範囲外を `stimulus_for()` で塞いだのと同じ理由だが、**あのときは落ちた1箇所だけを塞いで同種の穴を探さなかった**。あわせて、読み込みで止める以上 `validate` には届かなくなった検査（`_check_slots` の範囲外・抜けの分岐、設問IDへの `_check_unique_ids`）を削除した——一度も発火しない検査を残すのは「設定したつもり」と同じ（`simultaneous` で `slot` を書いた場合の分岐は、ローダーがそこを飛ばすので今も到達する。残してある）。**(5) この修正中にもう1件見つかった**——`sample_overlap: disjoint` に `stimuli_per_persona: 2` を書くと `stimuli_count_per_persona()` が矛盾を無視して `1` を返すため、読み込みガードが「m=1 なのに slot 2 の設問がある」と**設問を指して**停止し、直すべき `design` の行に辿り着けなかった。同メソッドが矛盾を検出して送出するようにし、規則の定義を1箇所に集めた（ローダーは例外を捕まえて自分の判定を飛ばし、`validate` が `_check_design()` で本来の E6 を報告する）。検証は `ruff check` パス・**非 Spark テスト633件全件パス**・**Spark テスト82件全件パス**（1回目は 2 failed で、`test_panel_spark` のひな形が `sample_overlap` を変えるのに設問は3 slot のままだった。直前に他3ファイルの同じ問題を直した際、**同じひな形を使うファイルを確認しなかった**のが原因）。「`all` が他の設定に左右されない」ことを `rotation` を変えても結果が同じであることで固定し、設問IDの重複・`slot` の抜け／範囲外・`design.memory` が**読み込みの時点で**止まることも固定した。`examples/survey_sample*.yaml` はどちらも `validate_static` を通る。**Databricks 実環境での確認は未実施。**

### 36. 調査定義の書き方ガイドを追加（実例を自動検証つきで）
- **日付**: 2026/08/08 17:00
- **概要**: 調査定義の書き方が `SPEC.md` §3（フィールドの網羅的な一覧）・`examples/*.yaml`（注釈つきの実例）・ローダーの停止メッセージ・`AGENTS.md` の不変条件に分散しており、**利用者向けの手引きが無かった**。仕様書は実装の根拠として書かれていて設計判断の理由が本文の大半を占めるため、書き方だけ知りたい人には重い。`docs/GUIDE_SURVEY_DEFINITION.md` を新設し、最小の定義から順に足していく形で、設問の slot 展開と `measure`・設問ごとの `remember`・スクリーニング3方式・提示設計2軸・よくある止まり方・実行手順を並べた。**実例は自動検証している**（`tests/test_guide_survey_definition.py`）——ガイド中の ```yaml ブロックを抜き出し、完全な調査定義（トップレベルに `survey:` を持つもの）はそのまま、`screening` / `design` だけの断片は最小の定義に差し込んで `validate_static` を通す。断片も対象にしたのは、**方式ごとに書けるものが違う**箇所（`ask` に `conditions` を書くと止まる 等）が完全な定義しか見ていないと検証されないため。落ちたときはガイドの行番号を出す。ブロックを1件も拾えなくなったときに空振りで緑になるのを防ぐため、拾えた件数の下限も別テストで押さえている。**この網を入れたのは、`examples/survey_sample.yaml` が「読めることしかテストしていなかったせいで、割り付けの合計が `panel.size` と一致しないまま放置されていた」（項目34で発覚）のと同じ轍を踏まないため。** 意図的に例を壊すと該当行を指して落ちることを確認済み。あわせて `README.md` / `SPEC.md` §3 / `AGENTS.md` からガイドへ辿れるようにした。検証は `ruff check` パス・非 Spark テスト640件全件パス（新規7件）。

### 37. 実行プロンプトの記録を1ペルソナの全設問ぶんに広げる
- **日付**: 2026/08/09 00:10
- **計画**: [docs/history/20260809-001058-prompt-sample-all-questions-plan.md](docs/history/20260809-001058-prompt-sample-all-questions-plan.md)
- **結果**: [docs/history/20260809-001058-prompt-sample-all-questions-result.md](docs/history/20260809-001058-prompt-sample-all-questions-result.md)
- **概要**: `run_metadata.json` / `prompt_sample.md` に残る「実際に送ったプロンプト」が**1ペルソナの1設問**だけで、2問目以降に何を送ったのかを事後に確かめる手段が無かった（`_pick_prompt_sample()` が先頭1セッションを採り、`sample_prompt()` がその `units[0]` だけを組み直していた。既定の `remember` 無しでは1設問＝1セッションなので、結果として1問しか残らない）。提示順が定義順に化けていた不具合（項目33 の H2）を記録から検知できなかった原因でもある。**1名ぶんという方針は変えずに、その1名については全設問を ask order で残す**ようにした。`units[0]` に限っていたのは、記憶を持つ設問のプロンプトに入る先行設問の**回答本文**がエンドポイントの出力で純関数からは組み直せないためだったので、書き出し済みの `responses.answer_raw` を読んで再生する形で解いた（`_recorded_answers()`。再開実行でも前回の回答が読めるので欠けない）。あわせて記録の単位を `system` / `user` の2本から**メッセージ列そのもの**に改めた——記憶を持つ設問は `[system]`/`[user]`/`[assistant]`/`[user]` … という列で送るので、2本には潰せない。回答を引けなかった設問は目印（`MISSING_ANSWER`）で埋めたうえで `missing_answers` に残す。黙って `[assistant]` を落とすと列の形が変わり、「実物と一致する」という記録の前提が読み手に分からないまま崩れるため。**`prompt_sample` の構造は破壊的変更**（`{persona_uuid, questions: [{stimulus_id, question_id, sequence, messages, missing_answers}]}`）で、`runs` テーブルの `metadata_json` 列は1名×全設問ぶんだけ太る（メタデータだけを受け取った相手にも「どう聞いたか」が伝わるようにするため、全文を入れる方針を採った）。`SPEC.md` §7.4・§9 と `notebooks/quickstart.ipynb` の説明を追随させた。検証は `ruff` パス、非 Spark テスト**649件全件パス**（変更前640件、9件追加）。追加分のうち9件は Spark 不要で、`_RecordingClient` が実際に送ったメッセージ列を控え、組み直した記録と突き合わせる形にしてある（記憶なし・記憶ありの両方）。Spark テストは既存4件を新構造に合わせ、2件追加した（全設問が ask order で並び各設問の `stimulus_id` がそのペルソナの `assigned_stimuli` の slot 番目と一致すること／記憶ありで実送信と一致すること）。この環境には Java 21 があるため Spark テストも手元で実行し、**84件全件パス**（変更前82件）を確認した。

### 38. 調査設計ページの見積もりを実測ベース（回答件数単位）に置き直し、予算目安を追加
- **日付**: 2026/08/10 09:35
- **計画**: [docs/history/20260810-093530-estimate-budget-params-plan.md](docs/history/20260810-093530-estimate-budget-params-plan.md)
- **結果**: [docs/history/20260810-093530-estimate-budget-params-result.md](docs/history/20260810-093530-estimate-budget-params-result.md)
- **概要**: 3回分の実測（`docs/issues/調査設計ページの提示内容改修.md`）を「5. 確認して開始」に反映し、トークン数・単価・実行速度を設定パラメータにした。**根っこの不具合は換算の単位**——実測値（1,600入力 / 15出力 / 1,190.6件/分）は**1回答あたり**の値なのに、`estimate_cost()` はこれを `Estimate.sessions` に掛けていた。セッション数は `remember` の連結成分で設問をまとめて数えるので（`session_groups()`）、**記憶を持つ設問がある調査では回答件数より少なくなり**、掛けると出力トークンと予算がまとまり具合のぶんだけ過小に出る。現行 UI は `memory: none` 固定なので偶然一致していただけだった。`Estimate.answers`（`panel_size × 展開後設問数`）を足し、本調査はこれに、判定は1回で `batch_size` 人分を捌くので `screener_sessions` に掛ける形に分けた。**同じ dataclass を2つの単位で使い回すのをやめ**、`AnswerBenchmark`（`*_per_answer` / `answers_per_min`）と `SessionBenchmark`（`*_per_session` / `sessions_per_min`）に型ごと分割して、掛ける先を間違えたら型で気づくようにしている。所要時間は `平均レイテンシ × セッション数 ÷ model.concurrency`（960件/分相当）をやめ、実測スループット `回答件数 ÷ answers_per_min` に置換した（`estimate_cost()` の `concurrency` 引数は削除。`*_per_min` は並列度込みの実測値で、さらに割ると二重に効く）。**金額は画面の概算としてのみ出すことにした**——`Pricing`（USD 単価 ＋ 為替 ＋ 安全側の係数）を設定に置き、`docs/SPEC_UI.md` §3.4 の「金額は出さない」を差し替えたが、**`SPEC.md` §9 の `estimated_cost` は `null` 固定のまま**で、事前の目安と実請求を同じ数字として扱わせない線引きは残した。出すのは安全側の予算だけ（数字が2つあると、どちらを見たかで結論が変わる）。判定ぶんのトークンも合算する（issue の式は本調査だけだが、判定も実際に消費するので足さないと過小に出る）。注記に「入力トークンはコンセプト文の分量で増減します」を追加（issue の要望）。**記憶を持つ調査向けの実行時警告は入れていない**——現行 UI では実害が無いため利用者の判断で見送り、代わりに `AnswerBenchmark` docstring・`cost.py`・`Estimate` docstring・`ui_config.yaml`・`SPEC_UI.md` §3.4 の5箇所に「なぜ回答単位か、記憶を持つ調査種別を足すなら実績値の取り方から見直すこと」を残した。旧キーは黙って捨てず移行先を添えて停止する（`survey_session` は単位変更まで告げる。素通りさせると桁の合った過小見積もりになる）。速度・為替の 0以下と `budget_margin < 1` も拒否する。検証は `ruff check` パス・非 Spark テスト**661件全件パス**（変更前649件、12件追加。うち `test_cost_uses_answers_not_sessions` が今回の指摘そのものの回帰テスト）。issue の実例（200人 × 64コンセプト × 2問）と突き合わせ、回答件数 25,600・入力 40.96M・出力 384,000・21.5分・約 0.28円/件 が一致することを確認済み。**Streamlit 画面での目視確認は未実施**（この環境に Databricks 接続が無いため、数値は `estimate_cost()` の直接呼び出しで確認）。判定側の実績値は今回の実測対象外で、旧レイテンシからの換算のまま。

### 39. 設問ごとにシステムプロンプトを使い分けられるようにする
- **日付**: 2026/08/13 08:25
- **計画**: [docs/history/20260813-082512-per-question-system-prompt-plan.md](docs/history/20260813-082512-per-question-system-prompt-plan.md)
- **結果**: [docs/history/20260813-082512-per-question-system-prompt-result.md](docs/history/20260813-082512-per-question-system-prompt-result.md)
- **所見**: [docs/issues/202608131659.md](docs/issues/202608131659.md)
- **概要**: `[system]` が調査全体で1本しか持てず、購入意向と新規性を別々の指示で聞けなかった（起票者の実測では書き分けたほうが精度が高い）。`main_survey.prompt.systems` に `{名前: 文面}` を並べ、`questions[].system` にその**名前**を書いて紐づける形にした。**設問に書くのは名前で、文面ではない**——設問はコンセプト数だけ `slot` 展開されるので、文面を持たせると同じ長文が展開数ぶん複製され、直すときに1箇所取りこぼすと案によって違うプロンプトで聞いたことになる。**未定義の名前を指したら読み込みで停止する**（`_reject_unknown_system_prompts()`）。`run` は `validate` を通らずに実行できるので、綴り違いを既定へ黙って落とすと、書き分けたつもりの設問が既定の `[system]` で聞かれ、実行後に `prompt_sample.md` を読み比べるまで気づけない。逆向き（書いたのにどの設問も指していない）は結果の意味を変えないので `W_UNUSED_SYSTEM_PROMPT` の警告に留めた。**記憶（`remember`）を跨いでよく、そのとき `[system]` はいま聞いている設問のもの**にした（`replayed_messages()` の3つ目の不変条件）。先行設問のものを使うと、これから答えさせる設問が別の指示で聞かれることになる。列は毎ターン組み直す方式なので、先行ターンの回答は `[assistant]` として残ったまま `[system]` だけが替わる——`examples/survey_sample.yaml`（`q_novelty_1` が `q_intent_1` を覚えている）がその実例になっている。混在を停止させる案は、まさに issue の並び（購入意向を覚えたまま別プロンプトで新規性を聞く）が書けなくなるため採らなかった。**prefix cache は損なわれない**——記憶を持たない調査の投入順は `(stimulus_id, question_id, persona_uuid)` なので同じ設問＝同じ `[system]` が連続する（§6.2 に明記）。`app/config/ui_config.yaml` に `purchase_intent` / `novelty` を追加して既定2問に紐づけたが、**UI 側のコードは1行も変えていない**（`survey_design.py` と `_expand_questions()` の辞書展開が `system` をそのまま運び、`uiconfig/loader.py` は `prompt` を素通しの `Mapping` として扱うため）。この素通しが効いていることは slot 展開後の設問に `system` が残るテストで固定した——落ちると1案目だけ書き分けが効く形になり症状が出にくい。**画面には出していない**（文面は調査ごとに変える値ではなく運用側が固定するもので、毎回入力させると消し忘れ・貼り間違いの余地が増える）。検証は `ruff check` パス・非 Spark テスト**673件全件パス**（変更前662件、11件追加）。`tests/` だけ変更前に戻して 662件パスすることを確認し、既存の調査定義の入力が1バイトも変わらないことを裏づけた。`examples/survey_sample.yaml` で `[system]` が実際に3通り（購入意向・新規性・既定）になることを目視確認済み。**`ui_config.yaml` の文面は暫定値**で、起票者が実測に使った文面への差し替えが要る。**精度が上がったかは測っていない**（書き分けを可能にするところまで）。**Databricks 実環境・Streamlit 画面での確認は未実施。**

### 40. 選択式設問に reasoning フィールド（理由を書いてから番号を選ばせる）
- **日付**: 2026/08/18 04:50
- **計画**: [docs/history/20260818-045040-reasoning-field-plan.md](docs/history/20260818-045040-reasoning-field-plan.md)
- **結果**: [docs/history/20260818-045040-reasoning-field-result.md](docs/history/20260818-045040-reasoning-field-result.md)
- **概要**: 「コンセプト調査で言語化させてから新規性を答えさせたい」への対応。自由回答設問を前に置いて `remember` で繋ぐ方式は今日でも書けるが設問が1つ増える。**同じ呼び出しの中で理由を書かせる**経路として、構造化出力のスキーマに `reasoning` を**番号より前のプロパティとして**足す `questions[].reasoning` を追加した（順序が意味を持つ——生成は前から進むので、先に置くことで「考えてから答える」順になる。後ろに置くと後付けの説明になり回答は変わらない）。理由は `responses.answer_reasoning` に残り、ローデータのダウンロードにも出る。**思考モード（`model.thinking`）は OFF のまま**で不変条件は変えていない。違いは、思考の内容が記録に残り、`model.max_tokens_reasoning` で予算を抑えられ、どの設問で使ったかが `run_metadata.json` に残ること。決めごとが3つ。**(a) `structured_output: always` を必須にした**（`validate` がエラー）——`auto` のフォールバック（正規表現パース）は最初に現れた数字を採るので、理由文の中の「205円」を回答番号として拾い、パース失敗がもっともらしい回答に化ける。あわせて reasoning 設問では数字の走査そのものを止め、JSON の `answer` からしか番号を採らない。**(b) `refusal` の判定から理由の本文を外した**——理由の散文はこちらが書かせたものなので「わかりません」が拒否の目印にならず（購入意向の既定選択肢に「わからない」がある以上、言い換えは高頻度で出る）、誤検出すると `QUALITY_FLAGS` 経由で「フラグ除外後の n」が実態より小さくなる。真の拒否は番号を返せないので `parse_error` で表面化する。理由の**外側**に書かれた前置き・説教は判定対象に残す。**(c) Web UI の既定設問は OFF のまま**——文面（`prompt.rules.reasoning`）と予算だけ `ui_config.yaml` に用意した。費用と所要時間が数倍になり、画面の見積もり式が合わなくなるため、既定 ON は実測の後で判断する。JSON Schema に `maxLength` は入れていない（strict 対応がエンドポイント依存で、拒否されると `always` の下では調査が止まる）。`responses` に列が1本増えるので `merge_upsert(evolve_schema=True)` を付け、`_apply_straightline()` の select を「テーブルが実際に持っている列」に限定した（書き込みが1行も起きない再開では列が増えないため）。**レビュー指摘への追加対応**: `reasoning_max_length`（文字＝モデルへの指示）と `max_tokens_reasoning`（トークン＝エンドポイント側の打ち切り）は単位も役割も違い二重管理ではないが、**整合を誰も検査していなかった**（既存の `max_length` / `max_tokens_open` も同様）。`validate` に `W_OUTPUT_BUDGET` を追加し、reasoning と open の両方で「指示した字数に対して予算が足りない」組み合わせを警告する。文字→トークンの換算は推定でしかないのでエラーにはしない。違いは `ui_config.yaml` / `examples/survey_sample.yaml` / ガイド §4 / `SPEC.md` §6.3 / `ModelConfig` の docstring に明記した。検証は非 Spark **951件パス**（変更前916件、35件追加）・`pytest -m spark` **106件パス**（変更前105件、1件追加）・`ruff check` パス。fake エンドポイントで 100人 × reasoning 設問を端から端まで通し、`answer_reasoning` が全件埋まることを確認済み。**精度の比較検証（現行／言語化設問／reasoning の3条件）は未実施**——実エンドポイントが要る。T2B の水準だけでなく案の順位・選択肢分布の形・ペルソナ間の分散・セグメント間の差を見る必要がある。**CI は未確認**。 **追補（`prompt.rules.reasoning` を設問タイプごとの入れ子に）**: 指示行を `rules.single` と同じ並びに**1本だけ**置いていたため、置き換えたときに設問タイプ固有の指示が落ちていた——`type: multi` かつ `reasoning: true` の設問は「当てはまる番号をすべて、カンマ区切りで答えてください。」が消えて「そのうえで当てはまる番号を選んでください。」だけになり、複数回答なのに1つだけ選ばせる問いになっていた（構造化出力のスキーマは `{"answers": [...]}` のままなので、パースでも集計でも表面化しない。問いかけだけが変わる）。`rules.reasoning` を `single` / `scale` / `multi` を持つ入れ子（`ReasoningRules`）に変え、設問タイプの指示行と1対1で対応させた。**設問定義側は変えていない**——`questions[].type` を `single` / `multi` / `reasoning_single` / `reasoning_multi` に畳む案は採らなかった。`reasoning` は回答の形を変えず（`answer_schema()` は型で器を決め、`reasoning` を1枚重ねるだけ）、enum に畳むと `crosstab` の `is MULTI` や `Question.is_ordinal()` の `is SCALE` まで展開対象になり、**書き換え漏れがエラーではなく静かに違う集計値**（複数回答が単一回答として集計され構成比の分母が変わる）や不変条件のすり抜け（順序尺度のシャッフル禁止）になる。`REASONING_QUESTION_TYPES` は `ReasoningRules` のキーから引くようにして、指示行を持つ型と書ける型が別々に育たないようにした。**破壊的変更**: 1本の文で書いた旧形式（`reasoning: "…"`）は読まずに停止する（書き方を添える。黙って全タイプに使い回すと同じ事故が戻るため）。`ui_config.yaml` / `examples/survey_sample.yaml` / ガイド §4 / `SPEC.md` §6.3 を3キーに書き直し、UI 既定の `multi` には「すべて挙げる」旨を書き足した。検証は非 Spark **954件パス**（変更前951件、3件追加）・`ruff check` パス。`tests/test_jtbd_notebook.py::test_notebooks_do_not_ask_for_a_run_id[01_run_jtbd_research.ipynb]` は本変更の前から落ちており（JTBD notebook のウィジェット。今回の変更とは無関係）、そのまま。**CI は未確認**。

### 41. 結果閲覧は実行記録を「集計に要る範囲だけ」で復元する
- **日付**: 2026/09/02 08:34
- **計画**: [docs/history/20260902-083442-record-restore-plan.md](docs/history/20260902-083442-record-restore-plan.md)
- **結果**: [docs/history/20260902-083442-record-restore-result.md](docs/history/20260902-083442-record-restore-result.md)
- **概要**: 結果閲覧の「データを取得」が `SurveyDefinitionError: main_survey.prompt.rules.reasoning: …` で落ちていた。原因は `warehouse.fetch_survey()` が `runs.metadata_json` に残る定義を `survey_from_dict()` で**丸ごと再パース**していたこと——8/19 に `prompt.rules.reasoning` を設問タイプごとの入れ子にしたため（項目44の追補）、それ以前に実行した調査の記録が旧形式（1本の文字列）のまま残り、**集計に一切使われない文言キー**を理由に過去の結果が読めなくなっていた。書き込み側（これから実行する定義）の厳格さは正しく、誤っていたのは同じ厳格さを**実行済みの記録の読み直し**に当てていたこと。記録は事実であって、書き直させる相手がいない。`survey_from_record()` を新設し、境界を「集計の数字を決めるものか」で引いた——**読む**のは `survey` / `panel` / `stimuli` / `questions` / `output` とスクリーニングの方式で、ここは `survey_from_dict()` と**同じ厳格さ**（セル・目標人数・設問タイプ・選択肢・`measure` / `slot`・セグメント軸は数字の意味そのもので、黙って既定値にすり替えると誤った表を正しい表として読んでしまう）。**読まない**のは `main_survey`（model / prompt / persona_card）と `design` で、集計側に参照が無い。未知のキー・旧い書き方は無視する。これで同じ形の地雷もまとめて外れた（7/28 に廃止した `model.temperature`、7/30 に `main_survey:` 配下へ移した旧トップレベル `model:` / `prompt:`、`panel.screener`）。`panel.screener` は**方式だけそこから読む**ようにしたので、7/30 より前の記録でも通過率の注記が「未実施」に化けない。読まなかった block は既定値のまま入るため、復元した定義を実行や「何をどう聞いたか」の根拠に使わせない印として `SurveyDefinition.from_record` を足し、`model` には `RECORD_MODEL`（`endpoint: fake` / `deployment: ""`）を入れた。定義の全文は従来どおり `raw` に残り、`runs.metadata_json` は書き換えていない。検証は非 Spark **969件パス**（変更前952件、17件追加）・`ruff check` パス。旧形式の記録を組み立て、`build_result()` → `concept_axis_table()` / `stacked_crosstab_table()` → `labelled_csv_rows()` → `target_summary()` を端から端まで通して表が組み上がることを確認済み。既存の失敗3件（`test_jtbd_definition` と notebook 2件）は本変更の前から落ちており無関係。**Databricks 実環境・Streamlit 画面での確認は未実施**——実際に落ちていた調査で「データを取得」が通ることは利用者側での確認が要る。記録の読み直しでは未知のキーを弾かないので、**今後 `main_survey` 側に加える変更は結果閲覧では検知されない**（数字を決める block の厳格さを残すことで釣り合いを取っている）。

### 42. 投入する調査定義の複数行文字列を `|` で書き出す
- **日付**: 2026/09/03 08:44
- **計画**: [docs/history/20260903-084406-survey-yaml-literal-block-plan.md](docs/history/20260903-084406-survey-yaml-literal-block-plan.md)
- **結果**: [docs/history/20260903-084406-survey-yaml-literal-block-result.md](docs/history/20260903-084406-survey-yaml-literal-block-result.md)
- **概要**: `ui_config.yaml` に `main_survey.prompt.systems.purchase_intent` を書くと、Volumes に置かれる調査定義 YAML が `\n` エスケープの1本の長いスカラーに潰れていた。原因は書き出し側で、**PyYAML は `default_style` 未指定のとき自分からはブロックスタイル（`|` / `>`）を選ばない**——plain → single-quoted → double-quoted の順にしか試さない。設定側で `|` を使って書いても、読み込んだ時点でただの改行入り文字列になり「`|` で書いた」情報は残らない。さらに `purchase_intent` は箇条書きが `  - ` と字下げされており、PyYAML の `analyze_scalar()` は「改行の直後が空白で始まる」文字列に対して `allow_block_plain` と `allow_single_quoted` を両方 false にするため、**残る選択肢が double-quoted だけ**になっていた（字下げを外すと今度は single-quoted になり、改行が空行に化けて別の形で読めなくなる。設定の書き方では直らない）。**データは壊れていない**——`safe_load()` で往復すると元の文字列に完全一致し、ジョブが受け取るプロンプトは正しい。崩れていたのは見た目だけだが、この YAML は `jobs.py` の docstring が明言するとおり「ジョブが読むだけでなく、あとから人が『何を聞いたのか』を確認する対象」なので、その目的に対しては実害がある。`survey_yaml()` を `_SurveyDumper`（`str` の representer を差し替え、改行を含む文字列にだけ `style="|"` を指定）による `yaml.dump()` に置き換えた。`|` にできない文字列（行末に空白がある、`\r` を含む）は PyYAML が double-quoted へ自動的に戻すので強制指定でも壊れない（末尾に改行が無い場合は `|-`）。あわせて `width=4096` で既定80桁の折り返しを止めた——1行に収まるコンセプト文・設問文が途中で割れると、複数行を `|` にした意味が薄れるため。同じ経路を通る**コンセプト文（`stimuli[].text`）**と `screening.prompt.system` も併せて直った。`runs.metadata_json` の `survey_definition` は JSON なので対象外（`\n` が見えるのは仕様どおり）。検証は `test_jobs.py` **21件パス**（変更前17件、4件追加。リテラルブロックで出ること・往復すること・`|` にできない文字列でも往復すること・長い1行が折り返されないこと）、非 Spark 全件 **973件パス**、`ruff check` パス。既存の失敗3件（`test_jtbd_definition` と notebook 2件）は本変更の前から落ちており無関係（`git stash` して確認済み）。**Databricks への実投入は未実施**（この環境に接続が無いため、`survey_yaml()` の直接呼び出しで確認）。UI の「想定所要時間」が小さい N で常に 0 と出る件（`cost.py` の比例式に固定オーバーヘッドの項が無い／表示が分単位0桁）は実績値の取り直しが要るため本件に含めていない。

### 43. `research_system` からコンセプト調査一式を切り出す
- **日付**: 2026/09/04
- **概要**: `research_system` に同居していた2種の調査（コンセプト調査 `persona_sim` と JTBD 仮説探索 `persona_jtbd`）のうち、コンセプト調査だけをこのリポジトリへ移した。`persona_sim` は `persona_jtbd` を1行も import していない（言及は docstring の4箇所のみ）ため、パッケージと Web UI はそのまま持ち出せた。逆向きの依存は14系統あるので `research_system` 側は現状のまま残し、ここへは「コピー」している。移したのは `persona_sim/`（40ファイル）・`app/`（Streamlit UI）・`app.yaml`・`deploy/`・ノートブック2本・サンプル2本・仕様書・エージェント実行環境・CI・テスト36本。移していないのは `persona_jtbd/`・`schemas/`（3件とも `$id` が jtbd）・`config/jtbd_config.yaml`・JTBD のサンプル/ノートブック/仕様書/テスト10本、および JTBD の作業履歴8件と課題1件（対応する CHANGELOG 項目40〜43も削除し、以降を繰り下げた）。コピーだけでは済まなかったのは、`pyproject.toml` からの `persona_jtbd*` と `persona-jtbd` スクリプトおよび JTBD 専用の `jsonschema` 依存の除去、`AGENTS.md` の「2つの調査種別」節と JTBD 不変条件12件の削除、`README.md` の JTBD 節の削除、`persona_sim` の docstring 4箇所の書き換え、`deploy/README.md` への「`app.yaml` のカタログは移行前の値なのでデプロイ先に合わせること」の追記。あわせて `notebooks/run_survey.ipynb` の末尾の空セルを削除した（`test_token_usage_is_reported` が最後のコードセルを見るため、移行前から落ちていた）。検証は ruff パス・非 Spark 733件パス・Spark 85件パス。

### 44. コンセプト調査に要らないものを落とす（シンプル化）
- **日付**: 2026/09/04
- **概要**: 移行直後のコードから、コンセプト調査に不要なものを6段階で落とした。**本体 -2,118行 / テスト -1,277行**。**(1) 旧形式の移行案内の削除**——新リポジトリには旧形式の資産が無いので「そのキーはどこへ移した」という案内を持ち歩く理由が無い。`panel/schema.py` の9定数と `panel/loader.py` の参照9箇所、`uiconfig/loader.py` の旧キー案内3種を削除した（未知キーとして停止する挙動は `_reject_unknown` が変わらず担う）。現行形式の妥当性規則（`MISPLACED_SCREENER_KEYS` 等）と `AGENTS.md` が理由つきの停止を求めている `model.temperature` は残した。あわせて `uiconfig/loader.py` の `DEFAULT_CONFIG_PATH` が実在しないパス（実体は `app/config/ui_config.yaml`）を指しており引数なし呼び出しが必ず失敗していたのを、既定を持たせず `path` を必須引数にして解決経路を1本にした。**(2) CLI 廃止**——`cli.py`(430行) と `__main__.py` を削除し、入口をノートブックと Web UI の2つにした。ノートブックは CLI を経由せず本体関数を直接呼んでいるためパイプラインは失われない。CLI と一緒に消えるところだった E1 の事前チェック（`validate_feasibility`）は `run_survey.ipynb` の検証セルへ移した。**(3) 集計表と xlsx の系統を1本に**——同じ数字を CLI 向け（表側＝セグメント）と UI 向け（表側＝コンセプト）の2系統で出していたのを UI 側に寄せ、`write_xlsx` と `write_ui_workbook` も `write_workbook` 1本に畳んだ。出力ファイル名が `crosstab_{measure}.csv` / `crosstab_all.csv` に変わる。**(4) `aggregates` テーブル経路の廃止**——集計結果を Delta に保存して読み戻す経路を削除し、読むたびに `responses` から数え直す形にした（保存した表と生データが食い違ったとき、どちらが正しいか決める手立てが無いため）。既定の `output.formats` は `("delta",)` から `("csv", "xlsx")` へ。**(5) 提示設計の凍結**——`sample_overlap` / `presentation` / `rotation` を削除し、反実仮想モナディック固定にした。`disjoint` / `allow_overlap` / `simultaneous` / `random` / `balanced` はどの経路からも実行されていなかった。`panel/assignment.py`(100行) は「定義順の配列をそのまま置く」8行になった。**(6) スクリーニングを1方式に**——`ask` / `assume` を削除し、判定用 LLM による方式だけを残した。**本人に設問で聞く実測スクリーニングが無くなるので、インシデンスの実測値は取れなくなる**（`runs.screener_incidence` を廃止し、`screener_incidence_estimated` のみ）。`screening:` を書かない経路は従来どおり使える。`AGENTS.md` の不変条件も更新した（disjoint の均等割り当て → 提示設計を選べるようにしない、assume/infer のインシデンス2件 → 通過率を実測値として扱わない1件）。検証は各段階で ruff パス・非 Spark 660件パス・Spark 81件パス。

### 45. 親仕様と構築仕様を1本の `SPEC.md` に統合する
- **日付**: 2026/09/04
- **概要**: `SPEC.md`（親仕様695行）と `SPEC_PHASE1.md`（構築仕様1254行）の2本立てをやめ、`SPEC.md` 1本にまとめた。親仕様のうちフェーズ2以降の構想・定性インタビュー・キャリブレーション章は落とし、**スコープ外（§14）とガバナンス・法務（§15）だけを取り込んだ**。§15 の節番号を変えていないのは、`persona_sim/__init__.py` の帰属表示・免責が `SPEC.md §15.1` / `§15.3` を根拠として参照しているため。構築仕様の §1〜§13 は番号を保ったまま、項目44 の削減に合わせて書き換えた——§2.5 `aggregates` を削除、§4.2 を1方式に、§5 を反実仮想モナディック固定（「選べるようにしない理由」を §5.3 に明記）、§7 を表2つと出力一式に、§10 を入口2つに、§12 を実装状況に、§13 の不変条件を更新。リポジトリ全体の `SPEC_PHASE1.md` 参照129箇所を `SPEC.md` に置き換え、`docs/SPEC_UI.md`・`docs/GUIDE_SURVEY_DEFINITION.md`・`README.md`・`AGENTS.md` の古い記述も直した。あわせて項目44(3) の削減で参照が無くなっていた `crosstab.concept_summary` / `ConceptSummaryRow` と `AggregateResult.topline` / `by_segment` を削除した（計算はされていたが、それを描く表がもう無かった）。検証は ruff パス・非 Spark 658件パス・Spark 81件パス。
