- 「ジョブ定義」などの言葉があるが、job/ジョブというと別の社内用語のイメージがあるため、survey/調査、という言葉にする。
- responsesとscreener_responsesのカラムにanswer_codeとanswer_codesの2つがあるが、2つは不要。answer_codesのみに統一する。
- プロンプト組み立てにおける[system]とペルソナカードをconfigで編集できるようにする。また、プロンプト組み立て時に「番号のみで答えてください。」といったrulesを付与しているが、これについても編集できるようにする。
- 実際に聞いている質問のプロンプトを把握したいと思われるため、実行後にどこかに1ペルソナだけ記録として残しておく。
- quickstartのnotebooksのジョブ定義にpromptを追加しておきたい。
- ジョブ定義内のimage_uri: null はblobパスとなっているが、databricks前提なのでカタログのURLベース
- スクリーニングは現在「ask」「assume」の2つのモードであるが、「ask」は費用面・実行時間や、実際の出現率との乖離などの問題がある。assumeは強制的にペルソナに属性を付与するため、矛盾が生じる可能性がある。この中間案として別のAIに複数のペルソナの情報を与え、条件合致の蓋然性が高いペルソナに対して調査対象の属性を付与するという方法が考えられる。これを実装してほしい。
- 
