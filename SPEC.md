# AIペルソナ調査基盤（仮称：Persona Lab）開発仕様書

| 項目 | 内容 |
|---|---|
| ドキュメント種別 | 親仕様書（spec） |
| バージョン | 0.2（フェーズ1構築仕様反映） |
| 最終更新 | 2026-07-28 |
| ステータス | 親仕様 SSoT（フェーズ1実装は [`SPEC_PHASE1.md`](SPEC_PHASE1.md) が優先） |

> [!IMPORTANT]
> **仕様の優先順位と構成の変更**:
> 本文書はプロジェクト全体の親仕様・中長期構想です。**フェーズ1の実装においては、構築仕様 [`SPEC_PHASE1.md`](SPEC_PHASE1.md) が優先**します。
> 開発に伴い、本文書における用語や仕様は一部更新されています。
> - **用語の統合**: 本文書の `job_id`, `job:`, `job_metadata` 等の用語は、実装では `survey_id`, `survey:`, `survey_definition` へ統一されました。
> - **提示設計の 3軸化**: 本文書 §16 に記載の旧 `design.type` (monadic 等) は廃止され、`SPEC_PHASE1.md` §5 の 3軸設計（`sample_overlap`, `presentation`, `memory`）へ全面移行しました。
> - **実行環境**: フェーズ1の推論基盤は Databricks Model Serving / Fake クライアントを採用しています。

---

## 1. 背景と目的

### 1.1 背景

電通の `People Model` / `People Research` に代表される「LLMで生活者ペルソナ群を仮想再現し、定量アンケートと定性インタビューをAIに実行させる」サービスが市場に登場している。これらは自社保有の大規模生活者調査データでLLMをチューニングし、大規模なペルソナ群に対してアンケートシミュレーションとデプスインタビューを実行する構成をとる。

一方で、外部サービスの利用には次の課題がある。

- 1調査あたりの単価が高く、探索的な試行（数十回の仮説出し）に使いにくい
- 自社の顧客データ・購買ログ・過去調査資産をベンダーに預けにくい
- ペルソナ生成ロジックがブラックボックスで、結果の妥当性を自社で検証できない
- 調査設計のイテレーション速度がベンダーのSLAに律速される

### 1.2 目的

上記を内製化し、**「仮説生成と事前スクリーニングを、実査の1/100のコストと1/50の時間で回せる状態」** を作る。

明示的に、本システムは実査の代替ではなく **実査の前段** に位置づける（§2.2、§14を参照）。

### 1.3 成功の定義

| 指標 | 目標値 | 測定方法 |
|---|---|---|
| 定量シミュレーションの再現性 | 過去実査20問について、選択肢構成比の平均絶対誤差（MAE） ≤ 8pt | §11 ホールドアウト検証 |
| 順位相関 | 選択肢のランキング一致（Spearman ρ） ≥ 0.75 | 同上 |
| 1調査あたりコスト | 1,000ペルソナ × 20問 で 3,000円以下 | 実測 |
| 所要時間 | 同条件で 30分以内 | 実測 |
| 定性の実用性 | インタビューログから抽出したインサイトのうち、実査で確認された割合 ≥ 40% | パイロット案件での事後照合 |

---

## 2. スコープ

### 2.1 In Scope

- ペルソナデータストアの構築（Nemotron-Personas-Japan + 社内データ）
- ターゲット条件に基づくペルソナ抽出・ウェイティング
- 定量調査シミュレーション（単一回答／複数回答／スケール／MaxDiff／コンジョイント簡易版）
- 定性デプスインタビューシミュレーション（1対1、ラダリング対応）
- 結果集計・可視化・レポート出力
- キャリブレーション／検証パイプライン
- 社内ナレッジのRAG取り込み

### 2.2 Out of Scope（非目標）

以下は **意図的にやらない**。要求されても断る。

- 実査の完全代替。統計的に有意な差の検定、市場規模の絶対値推計には使わない
- 個人の再現。実在する特定顧客の意思決定モデル化は行わない（倫理・法務リスク）
- 広告効果の絶対値予測（CTR、売上リフト等）。相対比較のみ
- 社外提供・SaaS化（フェーズ1〜3では自社内利用に限定）

---

## 3. 用語定義

| 用語 | 定義 |
|---|---|
| ベースペルソナ | Nemotron-Personas-Japan の1レコード。人口統計 + 性格特性 + 生活文脈のナラティブ |
| エンリッチドペルソナ | ベースペルソナに社内データ由来の属性（カテゴリ関与度、ブランド認知、購買頻度等）を付与したもの |
| パネル | ある調査のために抽出されたエンリッチドペルソナの集合 |
| 回答セッション | 1ペルソナ × 1調査票の実行単位 |
| ウェイト | 母集団構成比に合わせるための各ペルソナの重み |
| キャリブレーションセット | 実査結果が存在し、検証に使う設問群 |

---

## 4. ユースケース

### UC-1: コンセプトスクリーニング（定量）
新商品コンセプト案5本を、ターゲット層1,000ペルソナに提示し、購入意向・独自性・共感の3指標で相対評価。実査にかける案を2本に絞る。

### UC-2: ターゲット探索（定量）
「誰に刺さるか」が未定の商材について、全国代表サンプル3,000ペルソナに提示し、反応の高いセグメントをクラスタリングで発見。

### UC-3: インサイト深掘り（定性）
UC-2で発見したセグメントの代表ペルソナ20人にデプスインタビューを実施。「なぜその回答になったか」をラダリングで掘り、実査の調査票設計にフィードバック。

### UC-4: 調査票プレテスト
実査に出す調査票を先にAIパネルにかけ、設問の解釈揺れ・天井効果・選択肢の抜けを検出する。**これは本システムの最も費用対効果が高い用途と想定**。

### UC-5: 既存顧客セグメントの拡張理解
社内の顧客データから作った既存セグメント定義を、ベースペルソナ側にマッピングし「まだ顧客でないが似ている層」の生活文脈を記述する。

---

## 5. システム構成

```
┌────────────────────────────────────────────────────────────┐
│  Web UI (Next.js)  /  CLI  /  Python SDK                    │
└───────────────────────────┬────────────────────────────────┘
                            │ REST / SSE
┌───────────────────────────▼────────────────────────────────┐
│  API Layer (FastAPI)                                        │
│  ├ /panels      パネル抽出・ウェイト算出                     │
│  ├ /surveys     定量シミュレーション実行                     │
│  ├ /interviews  定性インタビュー実行                         │
│  ├ /analyses    集計・クロス・クラスタリング                 │
│  └ /validations キャリブレーション実行                       │
└───────────────────────────┬────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
┌───────────────┐  ┌────────────────┐  ┌──────────────────┐
│ Persona Store │  │ Simulation     │  │ Knowledge Store  │
│               │  │ Engine         │  │                  │
│ DuckDB /      │  │                │  │ pgvector         │
│ Parquet       │  │ ├ Quant Runner │  │ ├ 過去調査PDF     │
│ ├ base        │  │ ├ Qual Runner  │  │ ├ 商品仕様書      │
│ ├ enrichment  │  │ └ Weighting    │  │ └ カテゴリ知識    │
│ └ embeddings  │  │                │  │                  │
└───────────────┘  └────────┬───────┘  └──────────────────┘
                            │
                   ┌────────▼────────┐
                   │  LLM Gateway    │
                   │  ├ vLLM (local) │  ← logprob取得・大量バッチ
                   │  └ 外部API      │  ← 高難度な定性・要約
                   └─────────────────┘
```

### 5.1 モデル配置方針

| 用途 | モデル | 理由 |
|---|---|---|
| 定量回答生成 | ローカル（vLLM上のオープンモデル、20B〜30Bクラス） | ①logprobsが取得でき選択肢確率を直接読める ②数万セッションのバッチでコストが支配的になる ③社内データがプロンプトに入るため外部送信を避けたい |
| 定性インタビュー | 外部API（フロンティアモデル） | 対話の自然さ・追い質問の質が結果を大きく左右する。件数が少ないためコスト影響が小さい |
| モデレーター／要約／コーディング | 外部API | 同上 |

> **設計判断**: 定量で logprobs を使うのは重要。「サンプリングで100回振って構成比を出す」より「1回の推論で選択肢トークンの確率分布を読む」方が、コストが1/100かつ低確率選択肢の推定が安定する。外部APIの多くはlogprobsを返さないため、定量パスはローカル前提とする。

---

## 6. データ層

### 6.1 ベースペルソナ: Nemotron-Personas-Japan

- 提供元: NVIDIA / Hugging Face `nvidia/Nemotron-Personas-Japan`
- ライセンス: **CC BY 4.0**（商用利用可。ただし**クレジット表示義務あり** → §15.3）
- 生成方法: NeMo Data Designer。国勢調査等の実分布に統計的にグラウンディングされた合成データ
- 規模: データセットカードのviewer表記は約100万件、NVIDIAブログでは600万ペルソナと記載。**着手時に実ファイルで要確認**

#### スキーマ（実データより確認済み）

| カラム | 型 | 用途 |
|---|---|---|
| `uuid` | string | 主キー |
| `sex` | string | 男/女 |
| `age` | int64 | 年齢 |
| `marital_status` | string | 未婚/既婚/既婚(子供あり)/離別/死別 等 |
| `education_level` | string | 中学卒/高校卒/短大卒/高専卒/大学卒 文系・理系/大学院卒 |
| `occupation` | string | 業種＋規模＋役職（例:「小売業 大手」「介護福祉業 中堅」「(現在は引退)」） |
| `region` / `area` / `prefecture` / `country` | string | 地方 / 東西日本 / 都道府県 |
| `persona` | string | 総括的な人物要約（1文） |
| `professional_persona` | string | 仕事観・キャリア志向のナラティブ |
| `sports_persona` | string | 運動・身体活動のナラティブ |
| `arts_persona` | string | 芸術・創作のナラティブ |
| `travel_persona` | string | 旅行のナラティブ |
| `culinary_persona` | string | 食のナラティブ |
| `cultural_background` | string | 地域文化・世代背景 |
| `skills_and_expertise` / `_list` | string | スキル（文章／配列文字列） |
| `hobbies_and_interests` / `_list` | string | 趣味（文章／配列文字列） |
| `career_goals_and_ambitions` | string | 将来志向 |

#### ⚠️ 本データセットの制約（設計上の最重要ポイント）

以下は**含まれていない**。マーケティング調査に使うには社内データによる補完が必須。

- 世帯年収・可処分所得・世帯構成（人数、同居家族）
- 購買行動（カテゴリ購入頻度、価格感度、購入チャネル）
- ブランド認知・利用経験
- メディア接触（TV/SNS/動画の接触時間、利用プラットフォーム）
- 価値観の定量スコア（既存のセグメンテーション軸に接続できる形の数値）

また、性格記述（Big Five的な語彙）はナラティブ内に埋め込まれており構造化されていない。→ §6.3 で構造化抽出を行う。

さらに **合成データ由来のバイアス**に注意する。ナラティブが「伝統文化・地域行事・季節感」に強く偏っている傾向が観察される。都市部の20〜30代・単身・デジタルネイティブ層の表現が相対的に痩せている可能性があり、そのままだと若年層の反応が保守的側に歪む。§11の検証で必ず年代別誤差を分解して見ること。

#### 取り込み処理

```
1. HF から parquet をダウンロード → S3/MinIO に保管（バージョン固定）
2. DuckDB に取り込み、正規化
   - occupation を「業種 / 規模 / 役職 / 就業状態」に分解
   - age → 年代バケット（10代刻み、5歳刻みの両方を保持）
   - 全 *_persona カラムを結合した full_text カラムを生成
3. full_text を埋め込み化（多言語embeddingモデル）→ pgvector
4. 派生指標を算出（§6.3）
```

### 6.2 社内データの取り込み

3つのモードで扱う。混同しないこと。

#### モードA: エンリッチメント（ペルソナに属性を付与）

社内の集計済み統計から、ベースペルソナの属性キーに対して確率的に値を割り当てる。

```
入力例: 社内購買データの集計
  「30代女性・関東・大卒 → カテゴリX購入経験率 42%、月間購入頻度 平均1.3回」

処理:
  該当セグメントのベースペルソナに対し、
  category_x_user を確率0.42でTrue割り当て（層内でランダム）
  → 母集団の構成比を保存したまま個票にダウンスケール

出力: enrichment テーブル（uuid外部キー）
```

**重要**: 個人レベルの社内データをそのままペルソナに紐づけない。必ずセグメント単位の統計に集約してから割り当てる。個人の再現を避けるため（§2.2、§15）。

#### モードB: ナレッジ（RAGで参照させる）

商品仕様、カテゴリの市場構造、過去調査レポート、競合情報などをベクトルストアに入れ、**調査票の文脈情報**として提示する。ペルソナ属性としては扱わない。

- 想定ソース: 過去調査PDF、商品カタログ、ブランドガイドライン、カテゴリレポート
- チャンク: 800トークン / オーバーラップ 100
- 検索: ハイブリッド（BM25 + ベクトル）、上位5件

#### モードC: キャリブレーション（正解データ）

過去に実施した実査の**設問文・選択肢・回答構成比・サンプル属性構成**。§11で使う。**これが無いと本システムの信頼性は担保できない**。着手前に何本確保できるか確認すること。

- 最低ライン: 3案件 × 各20問
- 望ましい: 10案件 × 各30問、カテゴリを跨いでいること

### 6.3 派生指標の生成（前処理バッチ）

ベースペルソナのナラティブから、以下を一度だけLLMで構造化抽出し、テーブル化する。毎回の調査で推論するのはコストの無駄。

```json
{
  "uuid": "09ae83dd...",
  "big5": {"o": 0.72, "c": 0.85, "e": 0.31, "a": 0.68, "n": 0.44},
  "digital_literacy": 2,          // 1-5
  "price_sensitivity": 4,         // 1-5
  "innovation_adoption": "late_majority",
  "info_sources": ["テレビ", "地域の口コミ", "新聞"],
  "life_stage": "子育て終了期",
  "household_size_est": 2
}
```

- 抽出は構造化出力（JSON Schema強制）で実行
- 100件をサンプリングして人手検証、一致率80%未満なら抽出プロンプトを修正
- 全件処理は1回きり。バッチAPI等でコスト最適化

---

## 7. パネル抽出とウェイティング

### 7.1 抽出インターフェース

```python
panel = client.panels.create(
    name="30-40代女性・時短調理カテゴリ",
    filters={
        "sex": "女",
        "age": {"gte": 30, "lte": 49},
        "enrichment.category_x_user": True,
    },
    semantic_query="平日の夕食準備に時間をかけられない",  # ベクトル検索で加点
    size=1000,
    weighting="census_raking",   # none | census_raking | custom
    target_distribution="jp_female_30_49_2025",
    seed=42,
)
```

### 7.2 ウェイティング仕様

抽出結果は母集団構成比とずれる。特に `semantic_query` を使うと大きくずれる。

- **レイキング（IPF）** で、性別 × 年代 × 地方 × 学歴 の周辺分布を目標分布に合わせる
- 目標分布は国勢調査／労働力調査から作った参照テーブルを内部に持つ
- ウェイトのトリミング: 上下1%をクリップ、最大ウェイト比を10倍以内に制限
- 有効サンプルサイズ（Kish's ESS）を必ず併記する。ESSが名目Nの50%を切ったら警告

```
ESS = (Σw)² / Σw²
```

### 7.3 再現性

- `seed` 固定で同一パネルを再生成できること
- パネル定義とベースデータのバージョンをスナップショットとして保存

---

## 8. 定量シミュレーションエンジン

### 8.1 調査票フォーマット

```yaml
survey:
  id: "sv_2026_0726_concept_a"
  title: "冷凍時短調理コンセプト評価"
  context_documents: ["doc_prod_spec_v3"]   # RAGから注入
  stimulus:
    type: "concept"
    text: |
      【商品名】まるごと一皿
      【特徴】...
    image: "s3://.../concept_a.png"          # マルチモーダル対応時
  questions:
    - id: "q1"
      type: "single"
      text: "この商品を購入したいと思いますか。"
      options:
        - "ぜひ購入したい"
        - "やや購入したい"
        - "どちらともいえない"
        - "あまり購入したくない"
        - "まったく購入したくない"
      randomize_options: false      # 順序尺度は固定
    - id: "q2"
      type: "multi"
      text: "魅力を感じた点をすべてお選びください。"
      options: [...]
      randomize_options: true
      max_select: 3
    - id: "q3"
      type: "open"
      text: "そう思った理由を教えてください。"
      max_length: 200
```

対応タイプ: `single` / `multi` / `scale`(5,7,10点) / `rank` / `maxdiff` / `open` / `numeric`

### 8.2 実行フロー

```
for persona in panel:
    1. ペルソナカード生成（後述）
    2. 調査文脈（stimulus + RAG文書）を提示
    3. 設問ごとに逐次回答
       - closed設問: 選択肢トークンのlogprobsを取得 → 確率分布として保持
       - open設問: サンプリング生成（サンプリングパラメータは指定せず、エンドポイント既定に従う）
    4. セッションログを保存
```

### 8.3 ペルソナカードのプロンプト設計

```
あなたは以下の人物です。この人物として、調査に回答してください。

【基本情報】
{sex}・{age}歳・{prefecture}在住・{marital_status}・{education_level}・{occupation}

【人物像】
{persona}

【生活背景】
{cultural_background}

【仕事】
{professional_persona}

【関心事】
{hobbies_and_interests}

【この人物のカテゴリ関与】
{enrichment_summary}

---
回答時の注意:
- この人物の実際の生活実感に即して答えてください
- 調査に協力的すぎる態度をとらないでください。興味のない対象には
  率直に興味がないと答えてください
- 選択肢のうち1つだけを、番号で答えてください
```

> **設計判断**: 「協力的すぎる態度をとるな」という指示は必須。LLMペルソナは総じて肯定的に振れる（acquiescence bias）ことが知られており、購入意向のトップ2ボックスが実査の2〜3倍に出るのが典型的な失敗パターン。これはプロンプトだけでは消えないため、§11のスケール補正と併用する。

### 8.4 出力

```json
{
  "session_id": "...",
  "persona_uuid": "...",
  "weight": 1.34,
  "answers": {
    "q1": {
      "selected": 2,
      "probs": [0.08, 0.31, 0.39, 0.15, 0.07],
      "entropy": 1.42
    },
    "q3": {"text": "..."}
  },
  "meta": {"model": "...", "prompt_version": "v3", "ts": "..."}
}
```

`probs` を保持することで、①個票を確率的に扱う集計と ②決定論的な最頻選択の集計を後から選べる。エントロピーが極端に低いセッションが多い場合はモード崩壊のサイン。

### 8.5 品質フラグ

セッション単位で以下を判定し、集計時に除外できるようにする。

- ストレートライナー: 全設問で同一位置の選択肢を選んだ
- エントロピー異常: 全設問の平均エントロピーが下位1%
- 矛盾検知: 逆転項目（reverse-coded item）を1問混ぜて整合を見る
- 出力破損: パース失敗

---

## 9. 定性インタビューエンジン

### 9.1 構成

**モデレーターエージェント** と **ペルソナエージェント** の2エージェント対話。

```
Moderator(調査目的, インタビューガイド, これまでの会話)
   → 次の質問を生成
Persona(ペルソナカード, これまでの会話)
   → 回答を生成
   → 終了条件まで繰り返し
```

### 9.2 モデレーターの動作仕様

- インタビューガイド（大項目3〜5個）を順に消化する
- 各大項目について、**ラダリング**を最低2階層行う
  - 「なぜそう思いますか」→ 属性から便益へ
  - 「それが得られるとどうなりますか」→ 便益から価値へ
- 回答が抽象的・一般論的な場合、具体的なエピソードを要求する
  （「最後にそうした場面を、いつ・どこで・どうしたか教えてください」）
- 誘導尋問を避ける（「〜だと思いませんか？」の形を禁止）
- 終了条件: 全大項目消化 かつ 総ターン数 ≥ 12、または上限30ターン

### 9.3 ペルソナ側の制約

定量パスと異なり、こちらは**具体性を要求する**。

- 一般論ではなく、自分の生活の中の具体例で答える
- 知らないもの・使ったことのないものについては、知らないと答える
- 調査に迎合しない

### 9.4 分析

インタビューログ群に対して:

1. **オープンコーディング**: 発話単位でコード付与（LLM実行 + 人手レビュー）
2. **コード集計**: 出現頻度、セグメント別クロス
3. **バリューマップ**: 属性—便益—価値の連鎖を抽出し、ネットワーク図化
4. **代表発話抽出**: 各コードにつき3件

出力は Markdown レポート + 生ログ（CSV/JSON）。

---

## 10. 集計・分析層

- 単純集計（ウェイト適用／非適用の両方を出す）
- クロス集計（属性軸、エンリッチメント軸、クラスタ軸）
- コンセプト間比較（同一パネルで複数コンセプトを回した場合の対応のある比較）
- セグメント発見: 回答ベクトルに対するクラスタリング（k-means / HDBSCAN）→ 各クラスタのペルソナ属性を要約
- 自由回答のトピック抽出

**表示ルール**: すべての数値に「シミュレーション値」ラベルを付す。信頼区間ではなく **バラつき幅** と表記し、統計的推測の語彙（p値、有意差）は使わない。

---

## 11. キャリブレーションと検証 ★最重要

本システムの成否は、ここを作り込めるかで決まる。**フェーズ1で必ず実装すること。後回しにしない。**

### 11.1 ホールドアウト検証

```
1. 過去実査 N案件のうち、7割を較正用・3割を検証用に分割
2. 検証用案件について、当時のサンプル属性構成を再現したパネルを組む
3. 同じ調査票を実行
4. 実査の構成比と比較
```

**評価指標**

| 指標 | 定義 | 用途 |
|---|---|---|
| MAE | 選択肢ごとの構成比の平均絶対誤差 | 全体精度 |
| Top-2-Box誤差 | 意向系設問の上位2選択肢合計の誤差 | 実務で最も見られる指標 |
| Spearman ρ | 選択肢順位の一致度 | 相対比較の妥当性 |
| セグメント別MAE | 年代 × 性別で分解 | どの層で歪むかの特定 |

### 11.2 スケール補正

生の出力は肯定側に偏る。較正用データから補正関数を学習する。

```
方法1（推奨・単純）: 設問タイプごとの単調写像
  意向5段階について、シミュレーション構成比 → 実査構成比 の
  等調回帰（isotonic regression）を学習し、以降適用

方法2: プロンプト側のアンカリング
  「日本の消費者調査では『ぜひ購入したい』を選ぶ人は
   通常5〜10%程度です」という分布事前情報をシステムプロンプトに入れる
```

方法1と方法2は併用可。ただし補正後の値は必ず「補正済み」と記録し、生値も保持する。

### 11.3 継続的検証

- 新しく実査を行うたびに、その案件をキャリブレーションセットに追加
- 月次で全ホールドアウトを再実行し、精度ダッシュボードを更新
- ベースモデルを更新したら、必ず全検証を再実行してから切り替える

### 11.4 レッドライン

以下に該当したら、その用途での利用を停止する。

- 特定セグメントでMAEが20ptを超える → そのセグメントには使わない
- 順位相関が0.5を下回る設問タイプがある → その設問タイプを非対応にする

---

## 12. API仕様（抜粋）

```
POST   /v1/panels                パネル作成
GET    /v1/panels/{id}           構成比・ESS取得
POST   /v1/surveys               調査票登録
POST   /v1/surveys/{id}/run      実行（非同期・SSEで進捗）
GET    /v1/runs/{id}/results     結果取得
POST   /v1/analyses/crosstab     クロス集計
POST   /v1/interviews            インタビュー実行
GET    /v1/interviews/{id}/log   ログ取得
POST   /v1/validations/run       ホールドアウト検証実行
GET    /v1/validations/dashboard 精度ダッシュボード
```

すべての実行結果は不変（immutable）。再実行は新しいrun_idを発行する。

---

## 13. 技術スタック

| レイヤ | 選定 | 備考 |
|---|---|---|
| API | Python 3.11 / FastAPI | Databricks 実行環境（Apps・ジョブ）の Python バージョンに合わせる |
| 調査 | Celery + Redis | 数万セッションの並列実行 |
| ペルソナストア | DuckDB + Parquet（S3/MinIO） | 100万〜600万行の分析クエリに十分 |
| ベクトル | PostgreSQL + pgvector | ナレッジとペルソナ埋め込みを同居 |
| メタデータ | PostgreSQL | run、panel、survey定義 |
| 推論（定量） | vLLM | logprobs必須。GPU 1〜2枚から |
| 推論（定性） | 外部LLM API | |
| UI | Next.js + TypeScript | |
| 可視化 | Recharts / Plotly | |
| IaC | Terraform | |
| 実験管理 | MLflow | プロンプトバージョンと精度の紐付け |

---

## 14. 非機能要件

### 14.1 性能

- 1,000ペルソナ × 20問（closed中心）: 30分以内
- 並列度: 同時64セッション以上
- インタビュー1本（30ターン）: 3分以内

### 14.2 コスト

- 定量: 1セッションあたり 3円以下を目標（ローカル推論の償却込み）
- 定性: 1本あたり 100円以下

### 14.3 セキュリティ

- 社内データを含むプロンプトは外部APIに送らない（定量パスはローカル推論に閉じる）
- 定性で外部APIを使う場合、ペルソナカードから社内固有情報を除去するフィルタを通す
- 監査ログ: 誰がどのデータでどの調査を回したかを全件記録

### 14.4 可観測性

- run単位のトークン消費、レイテンシ、失敗率
- 品質フラグの発生率トレンド
- モデル／プロンプトバージョンごとの精度推移

---

## 15. ガバナンス・法務

### 15.1 結果の取り扱いルール

社内周知として明文化し、UI上にも常時表示する。

> 本ツールの出力はAIによるシミュレーションであり、実在する生活者の回答ではありません。
> 意思決定の根拠として単独で用いず、仮説生成・優先順位付け・調査設計の目的で使用してください。

- レポート出力には必ず本文言と、使用モデル・プロンプトバージョン・検証時点の精度を記載する
- 対外資料（クライアント提案、広報）への数値掲載は法務レビューを必須とする

### 15.2 個人データ

- 社内データはセグメント統計に集約してから使う（§6.2 モードA）
- 実在個人を特定・再現するペルソナは作らない
- 個人情報保護法上、統計目的での利用に該当することを法務で確認する

### 15.3 ライセンス表示（必須）

Nemotron-Personas-Japan は CC BY 4.0。**帰属表示が義務**。

- レポートのフッタ、システムのAboutページ、社外資料に以下を記載
  ```
  本システムは NVIDIA が公開する Nemotron-Personas-Japan (CC BY 4.0) を使用しています。
  https://huggingface.co/datasets/nvidia/Nemotron-Personas-Japan
  ```
- 派生データ（エンリッチドペルソナ）を配布する場合の条件を法務に確認する

---

## 16. 開発フェーズ

### フェーズ1: 基盤とPoC（〜8週）

- [ ] データセット取り込み、スキーマ正規化、DuckDB構築
- [ ] 派生指標の抽出バッチ（§6.3）
- [ ] パネル抽出 + レイキング
- [ ] 定量シミュレーションエンジン（single / multi / scale）
- [ ] **キャリブレーション検証パイプライン（§11.1）**
- [ ] CLIのみ。UIは作らない

**フェーズ1のゲート**: 過去実査3案件でMAE ≤ 12pt、Spearman ρ ≥ 0.65 を満たすこと。満たせない場合、原因を分解してから先に進む。ここを飛ばして機能追加に進むのが最大の失敗パターン。

### フェーズ2: 定性と社内データ統合（〜8週）

- [ ] インタビューエンジン（2エージェント対話）
- [ ] コーディング・バリューマップ
- [ ] 社内データのエンリッチメント機構
- [ ] RAGナレッジストア
- [ ] スケール補正（§11.2）

### フェーズ3: プロダクト化（〜10週）

- [ ] Web UI
- [ ] クロス集計・クラスタリング・レポート自動生成
- [ ] 精度ダッシュボード
- [ ] 監査ログ、権限管理
- [ ] 社内パイロット3案件

### フェーズ4: 運用（継続）

- [ ] 継続的検証の自動化
- [ ] MaxDiff / コンジョイント対応
- [ ] マルチモーダル（画像・動画コンセプト）対応

---

## 17. リスクと未解決事項

| # | リスク／論点 | 影響 | 対応方針 |
|---|---|---|---|
| R1 | キャリブレーション用の過去実査データが社内に十分ない | 致命的。精度を主張できない | 着手前に棚卸し。不足なら小規模実査を1本打ってでも作る |
| R2 | Nemotron の若年都市層の表現が薄く、その層で精度が出ない | 中〜大 | セグメント別MAEで早期検知。必要なら該当層のペルソナを自前生成して補完 |
| R3 | 肯定バイアスが補正しきれない | 中 | 絶対値でなく相対比較用途に限定する運用に倒す |
| R4 | LLMのモード崩壊で回答の多様性が出ない | 中 | エントロピー監視。ペルソナカードの情報量・プロンプト設計で調整（`temperature` は指定しない方針のため調整レバーに使わない。`SPEC_PHASE1.md` §6.3） |
| R5 | 「AIが調査した結果」として社内で過信される | 大 | §15.1の表示ルールを徹底。レポートテンプレートに強制挿入 |
| R6 | ベースモデル更新で精度が変動 | 中 | モデルバージョン固定 + 切替時の全検証を必須化 |
| R7 | データセットの実件数・更新頻度が未確認 | 小 | 着手時に確認。バージョンをS3で固定 |

### 未決事項（要判断）

- [ ] ローカル推論のGPU調達（オンプレ／クラウド、枚数）
- [ ] キャリブレーション用実査データの提供元部署と利用許諾
- [ ] フェーズ3以降の社外提供可否（提供する場合、ライセンス条件の再確認が必要）
- [ ] 既存の社内セグメンテーション体系との接続方法

---

## 参考

- Nemotron-Personas-Japan: https://huggingface.co/datasets/nvidia/Nemotron-Personas-Japan
- NVIDIA解説記事（日本語）: https://huggingface.co/blog/nvidia/nemotron-personas-japan-ja
- 電通 People Model / People Research（比較参照用）: https://www.dentsu.co.jp/labo/ai_for_growth/index.html
