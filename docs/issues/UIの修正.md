- 性別でも絞れるようにする。（UI上だけでなく、job側でも性別での絞り込みに対応していたかは要確認。）また、対象年齢はfrom-toを数値で入力する形にしたい。挙動としては、男性にチェックボックスをつける→男性の対象年齢をfrom-toで入力→女性にチェックボックスをつける→女性の対象年齢をfrom-toで入力という形にする。チェックボックスがつけられなければその性別は調査対象外。
- 「コンセプトを追加」ボタンは各コンセプトの一番下に置く。「最後の一軒を削除」ボタンは廃止し、削除ボタンを各コンセプトにつける
- ライトモードだと、各入力欄が白くて見づらい。現状、色がついてなくて平板なので、macOSのような雰囲気にする（ただし、複雑なCSSは不可。シンプルなもの、という前提で。）
- 

